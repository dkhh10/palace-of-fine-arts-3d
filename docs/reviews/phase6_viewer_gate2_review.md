MERGE WITH FIXES (1-4 before the Gate 2 numbers are quoted at the gate)

`phase6-viewer` @ 875d7d9 vs main, web/ + renders/web/gate2_* only, 29 files. Verified good: replace-not-blend holds (`replaced_glb_normal` 33, `kept_glb_normal` 0 — three has one normal slot, so replacement is structural), and `disposeOrphans` cannot free a live texture: `collectTextures` does NOT filter on `visible` while `materialsByDistance` does, asymmetric in the safe direction; no dangling reference, no double dispose. Colour spaces are right per map and KTX2 sRGB is not double-decoded — three 0.186 picks the sRGB ASTC internal format from `texture.colorSpace`, which pbr.js:150 sets before the first upload, so the override is the correct lever; every data map is `linear` in the manifest and `NoColorSpace` here. `normalScale` is +Y / OpenGL per rule 7 and all 60 scales are 1.0. Chunking keeps every instance (pbr_test.mjs asserts 180 == 180), recomputes each chunk's bounding sphere, is deterministic (stable median sort) and drops no batch when the budget is hit. No division in `instanceDistance` — a camera inside the sphere clamps to 0. Capture determinism: `?t=0`, `deviceScaleFactor: 1`, both placeholder sets off, PFA_QUERY deduplicated last-wins. `guard()` runs before BOTH Chrome sessions (G1-4); G1-2, G1-3, G1-6 and Gate 0 carry 11 are done. planned == loaded bytes exactly (469,837,957): the `uv1_in_glb:false` sets are excluded from the plan, so the bar reaches 100 %. Tracked files clean — 26 web files, largest new blob 1.64 MB (the six 1920x1080 stations), lockfile present, no node_modules/dist/public/assets, no pair-sheet PNG committed.

1. main.js:786 + 801 — **fix now**. The PMREM is counted TWICE: `scene.environment` IS `pmremTarget.texture` (main.js:412) and carries `image.width/height`, so `addTex` bills 3072x4096x8 = 100,663,296 B to `texture_bytes` and `addRT` bills the identical 100,663,296 B to `render_target_bytes`. Resident is 1 294.4 MB, not 1 395.0 (1080p 1 165.3, not 1 266.0). Fix: skip `scene.environment` in `addTex` when it is `pmremTarget.texture`, and correct the README table and the report.
2. gate2.sh — **fix now**. `check_manifest_files.py` is referenced by nothing (grep finds only its own docstring), so the capture cannot "refuse a half-baked set" as claimed. Fix: run it on `$MAIN/export/out/gate2/manifest.json` before the first `guard`.
3. .gitignore:40 — **fix now**. Ignores only `renders/web/gate1_pair_cam0*.png`; gate2.sh writes `gate2_pair_cam0N.png` (6 files, 20.8 MB) sitting untracked, one `git add -A` from the history. Fix: `renders/web/*_pair_cam0*.png`.
4. check_manifest_files.py:19 — fix now. Default path is the hard-coded main checkout. Fix: `os.environ.get("PFA_MAIN_ROOT", …)`, as gate2.sh:21 already does.
5. Backdrop — carry (bake/export, not the viewer). All ten `MAT_EXP_ENVBD__*` sets are `uv1_in_glb:false` and nine match no scene material, so the city backdrop takes NO Gate 2 data at all, and the two metalness maps (`backdrop_door_green`, `lamp_post`, factors 0.019 / 0.150) are wired neither as texture nor as factor. README states it; make it a named Gate 3 hand-off item.
6. README "texture memory alone is 921.4 MB against the manifest's 1 200 MB budget" — carry, not like with like: 921.4 includes ~190 MB the budget never counts (PMREM 100.7 + sky equirect ~90), and the budget's lines for assets that exist total 707 MB (539.4 + 147.9 + 20). Quote the comparable pair.
7. chunking.js:82 — fix now (latent). `makeChunk` copies TRS and `matrixAutoUpdate` but never `matrix`, so a source batch with `matrixAutoUpdate:false` would put every chunk at the identity. Fix: `m.matrix.copy( src.matrix )` after the TRS copy.
8. pbr.js:145-152 — carry. At `concurrency 3` two workers can both pass `! csOf.has( url )` on one shared file before either writes, so a real colour-space conflict goes unreported and the last write wins. Fix: set `csOf` before the `await`.
9. pbr.js:221 — carry. `applyFactors` applies the Gate 2 `normal.scale` when `set.maps.normalMap || m.normalMap`, i.e. onto a KEPT Gate 1 normal — Gate 2 data on a Gate 1 map, against rule 7. Inert today (all scales 1.0). Fix: drop `|| m.normalMap`.
10. pbr.js:143 — carry. `kept_glb_ao` is structurally always 0 (manifest.js drops `in_glb:true` occlusion entries, so `set.maps.aoMap` never exists and the skip branch is unreachable): the counter cannot evidence "ORN AO untouched" — it is untouched for another reason. Same report: 83 disposals with only 33 explained by `replaced_glb_normal`; itemise by slot.
11. screenshot.mjs:141-155 — carry. One page load then `__pfaStation(n)`, so the nearest-first order is computed for station 1 only, not per station as the README says. Also `qmap` lets `--query station=`/`size=` silently override `--stations`/`--size`.
12. pbr.js:281 `MAT_SLOTS` — carry. No `envMap`/`bumpMap`/`displacementMap`/`specularMap`, so a Gate 3 material using one would have that texture disposed while live. Add them before the Gate 3 bake lands. chunking.js:99 — same severity: `opts.maxChunks` is tested and named in the header but is in neither DEFAULTS nor the `?chunk=` parser; remove it or implement it.
13. Process — carry. Nothing stops a bare `node tools/screenshot.mjs` launching Chrome outside `chrome_run.sh` (the guard lives only in gate2.sh and the npm script) — exactly the reported 10 s slip during a bake. Put the pgrep / status.json guard inside screenshot.mjs.
