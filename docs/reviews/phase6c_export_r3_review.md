MERGE — phase6-export round 3, delta main..edaa437 (read-only; every number below re-derived from the committed code, the JSON on disk and my own glb/glTF decode). No blockers.

1. **Item 1 method holds.** `foliage_albedo_render.py:78-110`: `hide_render` on every object with no card material and on every non-mesh instancer, `film_transparent`, compositor detached, no denoise, uncompressed 32-bit multilayer EXR — so `Combined.A` is the cards' own coverage and `alpha>0.98` really selects solid card texels (log: 4 578 foliage objects, 8 materials).
2. **The ratios are computed, not typed.** `albedo_check.json` reproduces the claim exactly: boxes 0.977/0.871/0.975/0.894, materials shrub_light 0.982, shrub 0.917, shrub_dry 0.896, reeds 1.491 on 1 173 px = 0.63 % of shrub-box card pixels. `BOXES` (`foliage_albedo_check.py:59-68`) are byte-for-byte `qa_r16_probe.FOLIAGE16`; index map hue-validated, worst 11.7 deg.
3. **Translucent branch applied before the ratio, and it matches the shader.** `mat_build.py:1400-1412` is Principled(c) mixed with Translucent(c·mult) at `maprange(ttex,0.05,0.85,0.35,1.55)·translucency`; `shipped_albedo()` computes `base·((1-f)+f·mult)` from the same maps, `ALPHA_RAMP=0.15` matches `mat_build.py:1385`. Direction is safe twice over (Principled's specular/sheen layering can only make DiffCol ≤ base, pushing the true ratio lower). **The verdict "LIGHTING, not export" follows from the numbers.**
4. **Three confounders measured, not argued:** ramp-vs-MASK (`as_drawn_over_ramp` 0.991-1.001 on all 8), density footprint (`dense` k3/k5/k9: reeds moves +1 %), packed-vs-disk PNG (`uv_probe.json` max RGB and alpha delta 0.000000 on all 8). **MAT_reeds +49 % is honestly left open** (README 47: not fixed, no missing node, 0.6 % of pixels, scaling would be curve-fitting) — agreed.
5. *Carry:* the pass covers cam02/cam05 = 4 shrub boxes but **not QA station 01 (1.70x, the hero) or 03 (2.20x)**. That is the brief's scope, not an error, but README 47 should say the verdict is measured at 2 and 5 and inferred for 1 and 3.
6. *Carry:* the "shipped side" is a **recomputation** of `foliage_tex`'s formula from the untinted source at full res (`foliage_albedo_check.py:107-110`), not a read of the shipped `tex/*.png` or the KTX2 — a resample/quantise/UASTC error would be invisible (~1 %). Reword "the shipped tinted KTX2 albedo".
7. *Carry:* `foliage_uv_probe.py:82-85` histograms polygon **corner** UVs with `mod 1.0` (u=1→bin 0), so `uv_area_fraction` reads 0.002-0.005 for cards whose `uv_bbox` is provably [0,0,1,1]. The claim rests on the bbox, which is right; the fraction is meaningless as published — drop it or accumulate per-face coverage.
8. **Item 2 verified independently.** My decode: `env_trees_lod1.glb` 7 642 344 B, 32 meshes, 32 instanced nodes, 254 rows, attributes POSITION/NORMAL/TANGENT/TEXCOORD_0 — **no COLOR_0** (`-kv` correctly gated on `color0_meshes`, `gltf_pack.sh:167-171`). `trees_far_lod1.json`: 16 prototypes 25 604-29 984 tris, 472 626 unique, 127 placements, translation residual 2.9e-5 m. The 127 `(name, translation)` rows of `env_trees.gltf` and `env_trees_lod1.gltf` compare **equal** in my own diff, so the pre-pack assert is not vacuous; the post-pack check adds what gltfpack could still re-segment.
9. **Rev-2 AO contract untouched:** `env_trees.glb` re-packs to sha256 `7e17167d…`, a hash recorded in *main*'s `trees_far.py:286` before the refactor — byte-identity is verifiable, not asserted. `hand_off`/`color0` correctly suppressed for the walk-up set, so `topology.json` cannot be overwritten.
10. **`manifest_v4.py:842-899` invents nothing:** every field copied from the report, `placements` is `{count, same_as: trees.far_mesh.placements, verified_pre/post_pack}`, `lighting` says reuse verbatim, `bytes` matches the file. Only new constant is `draw_within_m = 15.0`, the brief's number.
11. *Carry (cosmetic):* `verify_glb.trees_lod1_order_check` sets `structure_matches` by substring-scanning the shared `bad` list (an unrelated "instanced nodes" failure flips it false), and `list(gi["attributes"].values())[0]` would IndexError on empty attributes; `if rw:` where the sibling uses `is not None`.
12. **Hygiene clean.** The GPU render went through `gpu_lock.sh run` (log line 1: pid + 1200 s registered, released by trap); `bake_queue/status.json` idle, no Blender live. The two 91 MB EXRs are excluded in `sync_main.sh` and under the `export/out/` ignore — untracked, confirmed. Branch touches only `export/` plus one 98 KB jpg, no other owner's file; `git merge-tree main edaa437` is clean.
13. *Carry:* the gate1 rsync still has no exclude for `env_trees_lod1.bin` (41 MB) / `env_trees.bin` (14 MB), pre-pack intermediates now copied into MAIN's untracked `out/` — same class as the EXR exclusion just added. *Nit:* commit `c995dbe` says "180-591 objects per shrub material"; `uv_probe.json` says MAT_shrub is 156 (180/187 are `objects_in_frame`); the README does not repeat it. *Pre-existing:* new code resolves MAIN via `gate0_common.MAIN_ROOT`, a hardcoded literal with no `PFA_MAIN_ROOT` fallback, unlike `foliage_tex.py:45` and `sync_main.sh:6`.
