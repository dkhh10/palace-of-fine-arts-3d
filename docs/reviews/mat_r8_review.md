MERGE WITH FIXES — the water table reproduces exactly from the committed renders; the coffer table does not, and both water sweep scripts are now silent no-ops.

Verified: withdrawn knobs leave no trace (calm floor back at 0.45, mat_build.py:891; murk gain ramp pushed to 30-90 m so the hero box at 23.4 m and the near water at 9.4 m both sit at round 7's 0.15, :996). Water acceptance table re-derived by me with `python3 scripts/mat_r8_measure.py` on the committed r8_cycles_* / r8_eevee_06 PNGs — all six rows match to the digit (water_refl 105.6 / 36.5 / 0.312 / +37.0). Coffer albedo: MAT_plaster_ceiling + _rib Base/Grey Color (mat_build.py:778-806); all four colours land at HSV sat 0.249-0.252 at hue 46.2/46.5/49.0 and hold Rec.709 luminance within 0.4 %, so "hue and luminance held, only chroma moves" is arithmetically correct. Ownership clean (only scripts/mat_*, docs/materials_notes.md, assets/materials.blend, renders/{logs,previews/materials,qa_comparisons/mat_r8_sheet.png}); mat_build.py:19 read_homefile(use_empty=True) = idempotent; library 41 materials, build_master 40 appended / 40 placeholders remapped, none unresolved; largest new binary is the owned assets/materials.blend 12.6 MB, everything else < 3 MB.

1. **fix now** — scripts/mat_r8_sweep.py:79-92 grabs `WATER_BUMP_DIST` and writes `dist.outputs[0].default_value`. That was a Value node when the sweep ran (f1a369b^); it is now a MapRange (mat_build.py:914), whose output default_value is ignored, so re-running the sweep renders four identical frames and prints a table that looks reproduced. scripts/mat_r7_sweep.py:96 has the same bug on `WATER_MURK_GAIN` (now MapRange at :996). Fix: `if node.type != 'VALUE': raise SystemExit(...)`, or drive the MapRange "To Max" input.
2. **fix now** — docs/materials_notes.md:1410-1415 (coffer field sat 0.966 -> 0.614 -> 0.499, rim 0.458, coffer/sky ratio 0.357): no committed script prints any of those. `mat_r6_measure.py coffer` prints only dark/light ratio + std (I reproduce 0.230 / 30.4 on the shipped frame), and the step-1 (albedo 0.40) frame was overwritten by the shipped one at the same filename. Name the box and script, or add the sat/ratio mode, before QA scores QA-06-8.
3. **fix now** — scripts/mat_r8_sheet.py:88 hard-codes `/Users/dk/Projects/3d render blender 3rd attempt building/reference/...` instead of `common.REFERENCE_DIR` / `$PFA_REFERENCE_DIR`. One line.
4. **carry** — `depth` is ShaderNodeCameraData "View Z Depth" (mat_build.py:858), i.e. distance from the rendering camera, not water depth and not WATER_Z. "The near field keeps round 7's calibration untouched" is therefore true of cam01 only: from cam05 and cam06 every water surface is past 24 m and gets the full 0.170 slope. That is visible in the owner's own table — cam05 lagoon sat 0.123 -> 0.471 and R-B +65.7 against ref 0.306; it clears the `>= 0.25` floor as written but is 1.5x the reference, so expect QA to call cam05 water over-warm. Restate the note's "24 m+ band" as "24 m+ from whichever camera renders".
5. **carry (phase 5)** — both new ramps (14-24 m, 30-90 m) are planes parallel to the image plane, so on the flythrough the ripple slope and murk gain sweep across a fixed patch of water as the camera dollies; expect pumping. A world-space term (distance to the hero station, or to the shore) would be stable; needs a check before the animation deliverable.
6. **carry** — no Eevee hero frame this round: the ramp was checked in Eevee only on cam06 (luminance, and Eevee/Cycles diverge hard there — sat 0.082 vs 0.302, R-B -9.3 vs -54.1) and cam04. Eevee SSR misses at high ripple slope fall back to the world probe, so hero water in the navigable viewport and the low-res Eevee flythrough test is unverified. One Eevee hero crop in r9.
7. **carry** — the coffer's Cycles value is not measured; "should land near 0.45" (notes:1417) is extrapolated from round 6's 0.052 Eevee/Cycles gap, while QA-06-8's blocker number was the Cycles 0.914. QA or r9 should render one Cycles cam04 before the box is called closed.
8. **carry** — the shipped ramp value (0.170 at 24 m+) was never rendered as a sweep case (cases were 0.030/0.070/0.130/0.220) and only w0 of the four sweep frames is committed, so the sweep table itself is not re-derivable from the repo. The shipped state is measured on its own acceptance frame, so this is bookkeeping, not a wrong number.
9. **nit** — scripts/mat_r8_probe.py is sound: it opens master.blend, takes the camera from the file (not a hard-coded station), raycasts real geometry and filters hits by material name `MAT_water*` rather than assuming a plane at WATER_Z; RES 1920x1080 matches the box space. It reads `obj.active_material` only, so a multi-slot water object would report "no water pixels" — loud enough, but worth a slot loop when ENV's water changes.

Also noted, already disclosed by the owner: the round spent 6 Cycles + 3 Eevee frames against a budget of 3 + 1.
