MERGE WITH FIXES — the gallery rig is real, built by `light_build`, saved in the asset at the shipped 1200 W and camera-invisible in the rendered frame; findings 1 and 2 must reach the lead BEFORE the next `lead_build.sh`, but nothing in the rig blocks the merge.

Verified clean. `build_gallery_fill` is called from `build(save=True)` (`light_build.py:992`) and is idempotent (prefix sweep + orphan-light removal, `:735-741`); the shipped `assets/lighting.blend` really carries 1200 W (binary probe: 17 float / 16 double `1200.0`, zero `2000.0`). `visible_camera` / `visible_glossy` are cleared at build (`:772-776`) and it shows — in `r17AFTER_AFTER_03c.png` the walk band peaks at 213/255 with 0.000 % of pixels over 250, so no emitter is in frame (BEFORE 212). One switching chain covers every path: `apply_gallery_for_engine` (`light_presets.py:172`) is called from `apply_shade_for_engine:251`, reached by `apply_final_cycles:307`, `apply_viewport_eevee:357`, `apply_preview_eevee:422` and `common.configure_cycles:340` — Cycles 1200 W, Eevee 0 W **and** `hide_render`. Eevee cost is genuinely zero (`after_pass.log` cam04 34.7 s / cam01 55.1 s vs the deleted-strip 35.4 / 54.8). Lamps carry no mesh, so the flythrough clearance casts are untouched. No file outside lighting's ownership.

1. `light_probes.py:224` + `light_presets.py:251` — **fix now, tell the lead**. `bake(physical_vault=True)` applies the CYCLES rig before the Eevee irradiance bake and now chains the gallery, so `lead_build.sh` will bake 16 x 1200 W into the volumes. The measured master was NOT in that state (BEFORE/AFTER cam03 `03e` bit-identical: 16.9, ridge 23.8/15.0), so §27.4's "EEVEE gets NONE of it" holds for the direct term only. Skip the gallery in the bake, or state it and re-measure cam03 Eevee.
2. AFTER pass level — **fix now (doc)**. `after_pass.log:1` reads `gal 1 (16 strips at 2000 W)`: every committed AFTER frame, `after_measure.log` and the sheet's AFTER tiles are the 2000 W rig while the asset ships 1200 W, and §27.6's item-2 cam03 rows (p95 89.0, 33.2, 6.6 %, 0.289, 0.305) come from an uncommitted bordered sweep. §27.6 discloses this for the hero wing rows only — say it per row, or commit one 1200 W cam03 log.
3. Missing logs — **fix now**. `light_r17_master.log`, `light_r17_gallery.log` and the sweep logs are cited by name in §27 but not committed, so the 5.5 % sky-visibility figure, both energy/colour tables, the Eevee A/B and the 9695 object count have no committed measurement; and nothing measures **ref 128's 0.292 / 0.661 / 0.307** at all (`light_build.py:424` points at a `light_r17_measure` header that has no such box) — the number that sets the shipped level. Commit the logs or give the ref boxes.
4. `light_build.py:428-430` — **fix now (doc)**. The shipped row is quoted wrong: "hue 34.7 -> 35.8 / 32.2 -> 33.4" and "ships at 0.356" are the (1.0,0.93,0.84) and r16 values; shipped is 36.3 / 33.9 and cam04 0.386 (§27.2, matching `after_measure.log`). `light_presets.py:173,249` still say "1000 W".
5. `light_flythrough.py:328` vs `:350` — **carry**. The new `SystemExit` duplicates a `RuntimeError` already in main on the same condition; the second is dead. Keep one.
6. Tracked binaries — **carry**. Net +43.3 MB, including two 8.2 MB 16-bit hero PNGs (over the 5 MB bar); e5dbb6e added ~25 MB of sweep frames that cf9d7e0 deleted (permanent pack cost).
7. `light_r17_sweep.py:299-305` — **carry**. `gal < 0` deletes the strips for the rest of the session and clears `_GALLERY`, so any later case silently has none. Assert it is the last case.
8. `common.configure_eevee` — **carry, lead's file**. Chains neither switch, so an Eevee render through it (e.g. `qa_render_round`'s fallback) on a Cycles-state file gets 16 x 1200 W lamps. Mirror `configure_cycles:336-341`.
