# MAT r9b review (delta d748c99..da7f829, branch `materials`)

MERGE WITH FIXES — the linear-space map is correctly built and correctly shipped, the transfer is measured at the real look, and every Round-9b table re-derives from a committed log. One physical fix is owed in round 10: the roll-off's safety argument is made in luminance and the map is applied per channel, so the finished albedo goes over 1.0.

1. **(1) clean.** The linearisation is inside `mat_projection.build` (exponent field, soft limit, geometric normalisation raised to `exp_calib`), and `PFA_photo_ratio.png` + `projection_meta.json` were rebuilt in the *same* commit as the code (1e58102); `assets/materials.blend` was rebuilt after it (9fea7cd) and nothing touched `mat_build.py`/the maps afterwards. Working tree is clean and all three shipped blobs hash-match HEAD. `meta.space` records the new contract.
2. **(2) consistent.** The exponent is looked up on the *render's* blurred display luminance (`Ld = lum(gauss(ren, LF_SIGMA))`), which is the base point the ratio photo/render is formed at — the right end. I checked the local-power form against the table's own exact scene-value inversion at the shaded attic: exact (with the same pedestal) 0.804 vs the shipped `linear_ratio` 0.8095, i.e. 0.7 %. So the 1.41 over-delivery is entirely the `f` pedestal, exactly as the notes say — not the base point. The notes' own instruction (set `f = 1` or re-justify it in round 10) is the correct one; it is documented as a deliberate choice, so it does not block.
3. **fix now (round 10) — (3) yes, the map can push albedo over 1.0.** `LIN_LIMIT_UP` 1.55 caps only the **LF luminance**, and three things come after it: the MF texture band (`S *= tex`, up to 1.62^0.5 = 1.27), the calibration normalisation, and the per-channel chroma residual — then the hard `RATIO_HI` 2.0 clip. Measured on the shipped map inside the band (mask is 1.0 over every band row): per-channel ratio reaches 2.00, effective shader multiplier (weight 0.6) up to 1.546 in luminance and 1.6 per channel. The 0.589 in the comment is `MAT_concrete_ochre`'s albedo **luminance**; its `Base Color` red is 0.748 and the `Albedo Tint` raises it to 0.777, so red passes 1.0 at an effective multiplier of 1.29 — which is precisely the drum case the comment cites as designed ("+80 % lands at +55 %" → red 1.03). Fix: clamp the finished albedo per channel in the `PFA_photo` mix, or apply `soft_limit` after `tex` and the normalisation with a per-channel ceiling. Also note `clipped_frac` is still 0.0399: 4 % of the band is hard-clipped at 2.0, which re-creates the plateau edge the roll-off was introduced to avoid, and no new seam frame was rendered this round (the sheet reuses the r9 seam pair). QA should look at the drum band on cam01/cam02.
4. **(4) yes.** `mat_r9b_agx.py:102-105` sets `view_transform = "AgX"`, `look = light_presets.LOOK` (= `AgX - High Contrast`, `light_presets.py:32`) and `exposure = -2.8331`; `renders/logs/mat_r9b_agx.log` prints `look 'AgX - High Contrast' exposure -2.8331`. The `sensor_fit = HORIZONTAL` fix is real and explains r9's negative transfers. The "do not ask for Punchy" reversal is measured at matched display luminance and agrees with `light_presets`. Finding 1 closed.
5. **(5) same value, same asymmetry as before.** `gloss_mix = 0.45` is one node value shared by both branches, but Eevee (`mat_build.py:1221`) still uses it only as the tint lerp, not as the reflection level; Cycles gets the level lift via the Fresnel-weighted mix shader. That is the r9 carry, unchanged and correctly commented — not a new defect.
6. **(6) clean.** Files changed by materials' own commits (a7fd80e..HEAD) are all owned: `scripts/mat_*`, `assets/materials.blend`, `assets/textures/projection/*`, `docs/materials_notes.md`, `renders/*`. `docs/decisions.md`, `docs/status.md`, `docs/briefs/`, `docs/reviews/` in the range came in through the `main` merge, not from this agent. No absolute paths in any changed script. Only new/changed binary over 5 MB is the owned `assets/materials.blend` (12.8 MB, unchanged size).
7. **(7) yes, every number is backed.** Acceptance table = `mat_r9b_acceptance.log` line for line; Photo 0/1 (135.6 → 119.4, −11.96 % vs −8.47 %, ratio 1.41) = `mat_r9b_phtest.log`, and `scripts/mat_r9_phtest.py` is now committed and reads its "asked" column from `projection_meta.json` (finding 4 closed); the constraint rows (p 1.25 hue binds, p 1.961 sat binds, sunlit at most 0.474 / +107.7) = `mat_r9b_constraint.log`; the transfer and four-look rows = `mat_r9b_agx.log`; 9679 objects = the master/phtest logs. `mat_build.py:180` now states the measured 0.461 / +104.7 and marks the old figure as the ask (finding 5 closed).
8. **carry** — `S *= tex` now happens *after* the calibration normalisation (round 9 normalised `raw`, which included the texture band), so the MF band's box mean is no longer absorbed by the calibration. Empirically ~0 here (sunlit 189.8 → 189.7), but it is a silent path for the calibration box to drift; normalise after `tex`.
9. **carry** — the map's median in-band linear ratio is 1.483 with p95 1.501: nearly half the band sits on the roll-off asymptote. Most of those pixels are sky/background that no surface samples, but it means the limit, not the photograph, sets the photometry over much of the band. Round 10's `f = 1` change lowers every exponent and will move this a lot — re-read `ratio_lum_p05_p50_p95` and `clipped_frac` after it.
10. **note** — `mat_r9_sheet.py`'s `--rows` guards use 2-space indentation inside `if` blocks (valid, inconsistent with the file). Cosmetic.
