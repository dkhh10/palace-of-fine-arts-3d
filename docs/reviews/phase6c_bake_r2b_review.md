# phase6-bake round 2b (3f2dc91..9844bc0) — MERGE WITH FIXES

Verdict: **MERGE WITH FIXES** — r2 findings 1-6 and 8 are genuinely closed in code and the re-run numbers replay from the shipped JSON; what is left is stale PROSE inside the hand-off file the viewer reads, plus one merge conflict to resolve in the export's favour. All four fixes are text-only; `trees_far_compose.py` is CPU-only, so re-running it costs no GPU.

Verified against MAIN `export/out/gate3/trees_far/`: `vertex_ao.npz` carries the npz key `topology_rev = [2]` (+ the `vertex_ao.json` sidecar); all 16 arrays are float32, length == `topology.json prototypes[p].verts` at rev 2. The faced-vertex statistics replay exactly from the npz (`nonzero ∪ zeros_faced`): means **0.2396-0.5047**, zeros 1.89-19.79 %, calibration 1.0 in all 16, `mean_all_verts` == the raw array mean to 5e-5; the `zeros_loose == loose_verts` assert (compose:105) is what makes that reconstruction legitimate and it holds on all 16 (10 meshes still have loose verts at rev 2). E_placement over the 127 placements: R **0.1374-4.9083**, G 0.1596-3.6934, B 0.1825-9.0654, 0 dark, 0 missing. The anchor is read, never recomputed (`trees_far_set.py:113-120`, hard stop if absent, z asserted 0) and the placement is `T(loc) @ S` on the export's already-anchored mesh with no second subtraction (:174); the assert is the placed **world bbox** against `topology.json`'s independently computed `placed_bbox_*` — worst residual **7e-05 m over 127/127**, and re-checking those meshes against the current rev-2 topology myself gives 5e-05 m, so the join-by-location claim is sound and not merely asserted. `proto` is in `BAKE_DIFFUSE_WORLD_KINDS` (gate3_common:88) and compose:166 asserts one world across all 20 records. `assert not missing_eb` is in (compose:157). `e_placement_topology.matches_current_rev: false` with the rev-1 vertex counts is honest and correct (the irr jobs predate rev 2; anchors are bit-identical across the two revisions — I diffed them, max 0). Only `export/` and `renders/logs/` touched, no binaries, `PFA_MAIN_ROOT` used throughout, queue `idle` with pid/expires_at null, no rc != 0 in 109 jobs, tfao 44 s / tfeb 158 s / tfirr 559 s. **Merges cleanly onto main** (`git merge-tree` clean, tree ed2f76b).

1. **fix now — the shipped hand-off file still describes the superseded anchor rule.** `trees_far_compose.py:321-323` writes `placement_transform = "...Translation(-anchor_p): the export bakes the prototype's own world matrix into the mesh and never subtracts it again... (and a defect reported for env_trees.glb)" ` — the exact claim r2 finding 1 killed, and it is in `instance_irradiance.json` today, where the viewer engineer will read it. Fix: restate as `T(trunk_base) @ S(s)` on the export's already-anchored mesh, anchor from `topology.json`, and re-run compose.

2. **fix now — `ratio.strength_decision` quotes the pre-anchor-fix measurement.** compose:257 (and `strength_shipped.note`) say 86.7 % of the hue gap, 205.5 -> 72.6 deg, k = 0.4386, B/G 0.041; the re-run `ratio_check.json` on the corrected anchor says **86.4 %, 205.5 -> 73.1 deg, k = 0.4506, B/G 0.0535**, ratio [1.0383, 0.9894, 0.1829]. The conclusion is unchanged (no hue overshoot, k still worse at 105.6 deg), but the numbers in the file no longer match the file beside it. Fix: quote ratio_check's own values in the same compose re-run; the lead appends the corrected figures under the 2026-09-17 decision entry.

3. **fix now — README:1790-1792 documents an assert the code does not make.** It says the new check is "XY centre within 0.05 m of `trunk_base`"; `trees_far_set.py:161-168` explicitly rejects that test (the reduced crown's centre sits 0.10-2.83 m off the impostor axis by construction) and asserts the world bbox against `placed_bbox_*` at 0.005 m plus the bottom z at 0.05 m. Fix: restate item 37's last sentence to the assert that exists.

4. **fix now at merge — `export/sync_main.sh` and `export/README.md` conflict with phase6-export (80730f5).** Bake is clean onto main, export is clean onto main, but the two conflict with each other: both append README items past line 1665, and both rewrite the same `PFA_SYNC_STATUS` block. Take **export's** version of that block — `[ "${PFA_SYNC_STATUS:-}" = 1 ]` — because bake's `[ -n "$PFA_SYNC_STATUS" ]` treats `PFA_SYNC_STATUS=0` ("I am not the queue") as true and copies the stale status over MAIN's GPU lock. README resolves by keeping both item sets.

5. **carry — the pidless lock is crash-safe, but only on the clock.** `gpu_lock.sh check` now reads MAIN and the worktree with effective-running winning (:51-92), which closes r2 finding 3; a bare `claim` records `pid: null` (:139) and `effective()` still returns idle once `expires_at` passes, so nothing wedges permanently. The cost is that a crashed agent holds the published GPU signal for the whole `secs` (default **1800**) with no pid to disprove it, and `guard` refuses other owners meanwhile. Fix: document that `run` / `PFA_LOCK_PID` is preferred and that a bare `claim` must pass an honest short `secs`.

6. **carry — nothing asserts the anchor is stable across the revision gap.** `e_placement_topology.note` justifies reusing rev-1 E_placement on the grounds that "the anchor and the 127 transforms are identical across revisions". True today (I checked: max anchor delta 0, max placed-bbox delta 5e-05 m), but it is prose, and it would read identically after a revision that *did* move the anchor. Fix: when `matches_current_rev` is false, compose asserts `trees_far_set.json`'s recorded anchors equal `topology.json`'s.

7. **carry — the hue verdict assumes the sign of the gap.** `trees_far_ratio_check.py:138` `passed = h_moved >= 0.5 * h_gap and h_after >= h_target`; with `h_before < h_target` the first term is trivially true and the second is the whole test. Fine for this scene, wrong if a future reference sits above the crown hue. Fix: compare on `abs()` with an explicit direction.

8. **carry — r2 findings 7 and 10 are unchanged and still open.** The LUT shaper constants remain a hard-coded copy (`trees_far_ratio_check.py:49-50` vs `gate0_common.SHAPER_*`), and `status.json` keeps only the newest record per job id, so the first-pass tfao/tfirr timings survive only in `renders/logs/6c_bake_run3.log` / `6c_bake_run4.log` — committed, so the evidence exists, but the superseded record is still not kept beside the new one.
