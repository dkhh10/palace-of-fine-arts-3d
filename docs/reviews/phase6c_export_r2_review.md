# phase6-export round 2 (d553a5a..80730f5) — MERGE WITH FIXES

Verdict: **MERGE WITH FIXES** — the far-tree work is real and replays from the committed code and the files in MAIN; 1-3 are cheap, CPU-only and should land before the viewer reads the manifest.

Verified against the artefacts, not the report. `env_trees.glb` is 3 699 324 B with **COLOR_0 on 32/32 primitives and no COLOR_1** (decoded the glb JSON chunk myself; 32 instanced nodes, 254 rows = 127 placements x 2 prims). `topology.json` is `topology_rev 2`, `card_select "stride"`, a per-prototype `anchor` + `anchor_check` (radius reproduced to <=1e-4 m against the manifest on all 16) and `placed_bbox_min/max` on all 127 rows; **`bbox_min.z - base_z` is 0.0000 on every one of the 16** — bake r2 finding 9 is closed, and finding 1's anchor is now the published one. Round-1 fix-nows 1-4 are closed: the tautologies are replaced by asserts on the *written* glTF (trees_far.py:615-639, worst 0.029 mm / 5.1e-7 scale over 127 rows) plus an evaluated-world-bbox check per row against the impostor quad (:446-478, worst XY 2.747 m inside radius); `leaf_area_kept` is `f.calc_area()` before/after (0.234-0.782) with the old estimate kept beside it; the stride is `(i*997)%1000`; the three colour spaces are asserted from `read_foliage.image_info`. The tint really is the node tree's (`params.json` -> `chain_to_image`, no typed constant) and `clipped_texel_fraction` is **0.0 on all eight** materials, hue/value matching the lead's entry (shrub 0.344->0.464, light 94.2/0.516, dry 36.1/0.600, reeds 0.651). 22 KTX2 / 13 001 325 B on disk, no `*_2048` anywhere, no `leaf_2k` in the manifest. The Decimate protection is by **membership** (`v.co.z > base_z + 1 m` at factor 1.0, trees_far.py:181-202) as the README says, and the force-kept lowest cards (43 + 7, willows only) are inside the same deterministic rev-2 build that produced `trees_far_lod2.blend`, so the index-addressed AO contract holds. The COLOR_0 promotion decodes each attribute out of the .bin and **asserts exactly one non-constant layer** (:671-674) — it fails rather than guesses; I checked the bufferViews carry no `byteStride` and the accessors are `normalized`, so the decode is sound. The rev gate refuses a mismatched value *and* a missing key (`ao_rev` stays `None` -> refuse, :514-533). `manifest_v4` invents no number: it copies through and asserts the loc join to 0.02 m. Only `export/` and `renders/logs/` are touched, no tracked binaries, every absolute path is a `PFA_MAIN_ROOT` default.

1. **fix now — manifest_v4.py:87-90: the impostor block quotes a formula whose constants it drops.** `impostor.how` is `irr["ratio"]["use"]` verbatim — "atlas_frame * clamp((E_placement / E_bake) ** strength, 0, clamp)" — but the block carries only `how`, `raw` and `prototypes`. `strength` 1.0, `clamp` 4.0, `zero_channel_fallback` 1.0 and `fallback` are in the bake's `ratio` and **not** in `manifest.json`, so the viewer cannot implement the sentence from the manifest and will guess or reopen the bake file. Fix: copy `strength`, `clamp`, `zero_channel_fallback`, `fallback` (and ideally `strength_decision`) into `impostor`.
2. **fix now — `materials.foliage` advertises 0 files / null bytes for a 13.0 MB set.** `foliage_tex.py` was re-run at 09:15, after `gltf_pack.sh --foliage` at 09:04, and it rewrites `foliage_tex.json` whole — so `ktx2_dir`/`ktx2_bytes`/`ktx2_files` are gone from both the worktree and MAIN copies, and manifest_v4.py:742-746 consequently wrote `bytes: null`, `files: 0`, `per_size_bytes {"1024": 0}` while 22 KTX2 / 13 001 325 B sit in `tex_ktx2`. Every budget number downstream is wrong. Fix: re-run `export/gltf_pack.sh --foliage` (CPU, seconds) and the manifest; and make `foliage_tex.py` merge into the existing report rather than clobber it, or have the pack write a sibling file.
3. **fix now — shrub_lod1's new assert has never executed, and its number has no artefact.** `export/shrub_lod1.py` changed at f95f316 (09:17), but `shrub_lod1.json` (08:10) and `env_shrubs.glb` (08:14) predate it: the shipped report has **no `placement_check` key**, so the `to_gltf` node assert, `PLACE_TOL_M = 0.001` and the `centre_delta_note` are untested code, and README:1776's "**0.050 mm** over the 1 376 shrub rows" is reproducible from nothing on disk (the figure appears only in that prose line, and in the lead's decisions entry). The geometry is unaffected — the glb is unchanged — but the claim is not backed. Fix: one CPU run of `shrub_lod1.py` + `gltf_pack.sh --shrubs`, then re-run manifest_v4; or delete the number.
4. **carry — round-1 finding 8 is fixed only where it was named.** `gltf_pack.sh:175-180` (`--trees`/`--shrubs`) now exits 1, but `--gate1` (:269-274) and `--gate0` (:347) still print to stderr and pack the PNG glTF at exit 0 — under the same manifest that advertises `ktx2_dir`, and for arch/orn/env/ground, i.e. the whole payload rather than the 3.7 MB side file. Fix: the same `exit 1` in both branches.
5. **carry — round-1 finding 6 is half fixed.** `extra_glb_check` now fails on a missing glb or report (verify_glb.py:145-155), but `shrub_lod1_order_check` (:87-88) still `return None`s when `instance_order_shrub_lod1.json` or `env_shrubs.glb` is absent, so a run without the order file still prints PASS with the LOD1 row order unchecked. Fix: append to `bad` there too.
6. **carry — the exporter logs "Mesh ... is not valid, and may be exported wrongly" for all 16 far-tree meshes** (`renders/logs/export_r2_rev2_color0.log`), once per prototype, and nothing in `trees_far.py` looks at it. Most likely the loose vertices bake r2 finding 5 counted (316 on willow_s11); harmless for shading but it means the joined datablock fails Blender's own validate before a 1 M-triangle export. Fix: call `me.validate(verbose=False)` after `join()` and record whether it changed anything.
7. **carry — two stale statements.** README item 45 still reads "Finding 9: DIAGNOSED, **not yet fixed**" immediately above item 46, which ships the fix in the same round; and round-1 finding 5 (`read_foliage.chain_to_image` walks unknown `bl_idname`s silently and pins `ShaderNodeHueSaturation` without checking its sockets) is still open, now that the tint it produces is what ships. Fix: mark 45 superseded; raise on an unknown node.
8. **Merge.** Clean onto main as it stands (`git merge-tree main 80730f5` -> tree, no conflicts). **After `phase6-bake` (9844bc0) merges it conflicts twice**, both trivially: `export/sync_main.sh` — both branches rewrote the same `bake_queue/status.json` guard block; keep bake's three new gate3 `--exclude 'trees_far/...'` lines *and* export's `[ "${PFA_SYNC_STATUS:-}" = 1 ]` test (bake's side still has the `-n` bug). `export/README.md` — both append at the tail after the `export/sync_main.sh` line (old line 1665): bake ~168 lines, export ~195; keep both, bake's block first. Bake's other README hunk (the `trees.far_mesh.lighting` section at :1065) auto-merges.
