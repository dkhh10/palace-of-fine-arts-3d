MERGE WITH FIXES — the library is safe and the acceptance table re-derives exactly from the committed frames; the round's two *conclusions* (QA-07-2 "unreachable", the projection's under-delivery) are measured through the wrong view pipeline and must not be acted on as written.

Verified by re-running the committed tools on the committed PNGs: `python3 scripts/mat_r9_measure.py hero r9_cycles_01… r9b_cycles_01…` reproduces every row of the item-A/B/C/F table to the digit (attic 189.8/36.1/0.461/+104.7, shaded 132.9, water_refl 110.3, near_water 118.3/208.5/0.234, std ratio 0.65/0.60, aniso 4.16/6.11); `seam cam02` reproduces 18.79 / 2.37 % / 3.265; `coffer` reproduces 0.339 / 0.625. `mat_r9_uvcheck.log` is a real proof: 33 meshes, 6242 loops, closed form vs `world_to_camera_view` **and** vs the baked `UVProj` both 0.0002 px, so "sampled from the world position, not the layer" is a genuine identity and is immune to an ARCH rebuild. Sweep shipped value cross-checks: the sweep's 0.00 → refl 102.1 and 0.45 → 116.3 bracket the shipped 0.25's measured 110.3. Ownership clean (mat_* scripts, docs/materials_notes.md, the reference_sheet licence line the brief asked for, assets/materials.blend, assets/textures/projection, renders/*); **assets/architecture.blend untouched**; no ARCH UV layer added; largest new binary is the owned materials.blend 12.8 MB, every other new file < 3 MB; `mat_r9_sweep.py:27` carries the value-node guard (r8 carry 1 fixed), `mat_r8_sheet.py:92` uses `arch_params.reference_dir()` (carry 3 fixed); no camera station copied from qa_cameras.py except the deliberate historical projector station.

1. **blocker (the conclusion, not the merge)** — `scripts/mat_r9_agx.py:18-19` builds an empty file and calls `common.setup_scene()`, which sets `view_transform` but never `look`; the log confirms `look None`. The project renders at **`AgX - High Contrast`** (`light_presets.py:32`, written into master by `apply_look`). The look is a contrast/chroma operator, and light_presets' own comment measures it as worth **R-B 94.8 (Base) vs 117.9 (High Contrast)** on this very box — more than the whole chroma gap. So the transfer table (0.19-0.24) is measured at Base and understates the shipped pipeline. Fix: set `scene.view_settings.look = light_presets.LOOK` and the hero's exposure before rendering the chart, then re-read the conclusion. Related: the notes recommend asking for `AgX - Punchy`, but light_presets measured Punchy at 112.7 vs the shipped 117.9 — that recommendation moves chroma *down*.
2. **fix now** — "QA-07-2 unreachable through albedo" is a non-sequitur even on the chart as rendered. The chart's base patch is sat 0.249; the hero is 0.461. Only the *slope* transfers, and applying the log's own 0.238 at lum 188 to the hero's actual pixel (R 227, B 122) says sat 0.461 → 0.53 needs display blue −13 %, i.e. scene blue ≈ **−55 %** — large but finite, not "of order −120 %, a negative albedo". What plausibly blocks it is the shaded attic's `sat <= 0.50` ceiling (transfer ~0.5 there, currently 0.394), and that is **unmeasured**. Restate the finding as "the shade window binds first" and measure it, or the lead routes a blocker to lighting on arithmetic that does not hold.
3. **fix now** — the ratio map is a **display-space** quotient (`mat_projection.py:157-159` divides two AgX-encoded PNGs) applied as a multiplier on the **scene-linear** albedo (`mat_build.py:53`). That is exactly why the owner's own `phtest` shows the shaded attic move 135.6 → 130.3 where the map asks −9.6 %: the projection delivers roughly a third of its own correction, by construction, not by a law of AgX. Fix is contained: raise the ratio to 1/transfer (or linearise both frames before dividing) inside `mat_projection.build`, re-clip, rebuild. Until then the notes should say "the map is built in display space and therefore under-delivers ~3x", not "AgX eats it".
4. **fix now** — `renders/logs/mat_r9_phtest.log` runs `/tmp/phtest.py`, an uncommitted throwaway, and it is the *only* evidence for the round's load-bearing Photo-0-vs-1 authority numbers. Same class as r8 finding 2. Commit it (or add `mat_r9_measure.py phtest`). Also: no committed log holds the acceptance / seam / coffer stdout — the scripts and frames are committed so I could re-derive it all, but per the process rule the tables should be teed into `renders/logs/`.
5. **fix now** — `mat_build.py:180` (the round-9 comment on `MAT_concrete_ochre`) claims the tint "lands the box at sat 0.59 / R-B 138 … with lum held at 190". The round's own measured result is **0.461 / +104.7**. That is an albedo-space prediction stated as a rendered fact in the shipped script; it will mislead round 10. Delete or mark it as the map's ask.
6. **carry** — the seam statistic is a **frame-wide max** of the blurred difference gradient (`mat_r9_measure.py:83-96`). The band mask ramps over 14-16 rows (`mat_projection.BAND`), so a full-amplitude ramped seam would read ~1.3 lum/px — well under the 3.27-3.74 the frame already shows. The metric therefore cannot *localise* or exclude a soft seam; it only bounds the worst step. It is a reasonable bound (nothing anywhere exceeds 3.7) and the x8 panels back it up, but QA should look at the known band rows specifically rather than trust the scalar.
7. **carry** — `Albedo Tint` (blue x0.758) is derived from **one box on one camera** and applied globally to `MAT_concrete_ochre`, `MAT_ornament_concrete` and `MAT_drum_band`, i.e. to every camera and every surface including the cam04 interior and the drum's bronze band, whose colour has nothing to do with the attic's correction. Only the hero was measured after it. Ask QA to check concrete hue on cam02/cam05/cam06.
8. **carry** — the projector station is hard-coded twice: `mat_build.py:71-74` and `projection_meta.json["projector"]`, and `mat_build` reads only `M_chroma` from the meta. If `arch_uvproj.py` is ever re-baked from the new cam01 the two silently disagree. One line: read `projector` from the meta the same way `M_chroma` is read.
9. **carry** — the coffer move rests on a gain ("the rib renders at 1.29 albedo points per rendered point") that no committed script prints, and the owner's own split reads rim 0.625 where QA reads 0.323 — the two statistics are anti-correlated (owner's rim > field, QA's rim < field), so they are not the same population and "over-reads" understates it. Direction is right (QA-07-9 says 0.323 is *under* the window); magnitude is unvalidated. QA must re-score on `r9a_cycles_04_rotunda_ceiling.png`.
10. **note, not a defect** — the maps ship at 1920x1080 against the brief's "one 4K ratio map + one 4K mask pack". Adequate for the 3840-wide final: the projection is a world-space projector (resolution-independent), the HF band is dropped so the content is band-limited to sigma 3 px, and at 4K it is a 2x bilinear upsample of an already-smooth field with 28-px mask ramps. 4K would have invented detail ref 169 does not carry at this scale. Accept the deviation; it is argued and measured.
11. **note** — constraint 1 holds numerically: sunlit lum 189.3 → 189.8 (window 178-201), columns and entablature held, and the map is mean-1 on the calibration box by construction. Nothing that passed before this round stops passing; the shipped `WATER_GLOSS_MIX` 0.25 is the conservative end of a swept, committed, four-frame table, and the Eevee branch gets the matching tint at the same effective strength (lerp(white, tint, 0.25) ≈ the Cycles grazing mix), though Eevee gets only the tint, not the level lift — the two branches agree on colour, not on mirror level.
