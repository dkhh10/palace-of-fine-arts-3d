# phase6-bake round 1 (main..b99dec1) — MERGE WITH FIXES

Verdict: **MERGE WITH FIXES** — the diagnosis verdict is sound and reproducible; fix 1-3 before the next run, 4-7 before the 16 `E_bake` jobs.

Verified good: the atlas-side numbers in decisions.md / README #33 are produced by the committed scripts, not typed. `impostor_diag_atlas.json` replays hue 149.4-257.4 deg, B/G 0.869-1.521, 40.0-62.6 % blue-max over 16 prototypes and the cam02 frame at 211.4 / 1.356 / display [17.8, 32.2, 38.8] hue 199; `impostor_diag_ref.json` replays [18.6, 17.6, 11.4] / 52.2 / 0.648 (so 147 deg and 3.4x are arithmetic on measured values). The 337 kB composite is the only binary and nothing outside export/ and renders/web/960 is touched — export/README.md is shared, so merge bake before phase6-export r2.

1. **blocker — export/gpu_lock.sh:22-46: a crash leaves `status.json` saying `running` forever.** No trap, no pid, no expiry: if Blender or the shell dies between `claim` and `release`, the one GPU-liveness signal CLAUDE.md allows stays `running` and the watchdog will not clear it (it kills pids, it never edits this file). Fix: `claim` writes `pid` and `expires_at` (same honest seconds as blender_run.sh), add `gpu_lock.sh run <secs> -- <cmd>` with `trap 'gpu_lock.sh release' EXIT INT TERM`, and readers treat `running` with a dead pid or a passed `expires_at` as idle. bake_queue.sh:162-179 has the same hole — carry to the export engineer.

2. **fix now — export/gpu_lock.sh:24-27: claim/release ignore who holds the file.** `release` during a live `bake_queue.sh` flips a real bake to `idle` (the viewer engineer then starts Chrome on the GPU); `claim` overwrites `current`/`note`, losing the running job id. Fix: refuse when the loaded record has `state == running` with a different `owner`, unless `--force`.

3. **fix now — export/imp_diag_view.py:281: the report is overwritten per run, and the numbers that carry the verdict survive nowhere.** `impostor_diag_view_<proto>.json` is one fixed path, so the 07:47 `sunonly,skyonly` run replaced the 07:44 `asis,diffuse` run; the file on disk holds only sunonly/skyonly and no log captured the rest. The headline claims (asis 213.2 / B/G 1.367 = "within 4 %", diffuse 215.6 / 1.431 = "+2.4 deg the wrong way") are unreproducible without re-spending the GPU. Fix: merge variants into the existing JSON instead of replacing it (or key the filename by variants) and tee the run into renders/logs/.

4. **fix now — README `trees.far_mesh.lighting.prototype_e_bake.bake`: reducer mismatch with the `instance` kind.** Shipped `instance_irradiance.rgb` is `mean_nonzero`, the mean over vertices that received light under the cut-out shadow override (gate3_instance_compose.py:84), not a plain vertex average. The spec says only "vertex-averaged", so a plain mean in the divisor biases every ratio by 1/cov. Say `mean_nonzero` over the same cov mask, and ship `cov` in trees_far/instance_irradiance.json as the shrub file does.

5. **fix now — same block: wrong geometry for the divisor.** E_bake is specified on "that prototype's LOD2 mesh", but the atlas was baked from the prototype object in gate3_imp.blend (job `imp_<proto>`, the `_LOD1` names in `impostors.prototypes`). The divisor must describe the body that produced the atlas: bake E_bake on that object; LOD2 belongs to the mesh path's `_IRRADIANCE`/AO only.

6. **fix now — the divisor is diffuse-only and the measured blue is not a diffuse term; verify on one placement before spending 16 jobs.** `sunonly` reads blue exactly 0.00000 although the rig carries three blue fill lamps (scripts/light_build.py:577, colour (0.03, 0.02, 1.00)) and warm lamps with B 0.68-0.88 (:344, :473), while `skyonly` reads B 0.387. Together those say the crown's blue is not a diffuse albedo response to punctual light but a specular/transmission response to sky *radiance*, which a per-channel diffuse *irradiance* ratio does not scale correctly. (Relatedly, decisions.md's "the leaf albedo carries no blue at all" does not follow from a sun-only render: that is albedo x lamp spectrum. The additivity conclusion — 100 % of the blue is the sky term — does stand.) Apply E_placement/E_bake to one far placement's atlas frame and show B/G moving toward the reference 0.648 first; if it does not, the impostor path needs a sky-occlusion term, not a ratio.

7. **fix now — same block, `use`: state that the ratio takes both values raw.** `instance_irradiance` is documented as "irradiance / pi (x lightmaps.scale = pi)". If the viewer feeds the scaled `_IRRADIANCE` into the numerator and the unscaled JSON `E_bake` into the denominator, every impostor is pi x too bright. One sentence: stored values on both sides, `lightmaps.scale` applied to neither.

8. **carry — absolute MAIN path, four times, without the house override.** imp_diag_atlas.py:33, imp_diag_ref.py:28, imp_diag_sheet.py:24, imp_diag_view.py:59 hard-code `/Users/dk/Projects/...`; every other export script uses `os.environ.get("PFA_MAIN_ROOT", "<that default>")` (gate3_instance_jobs.py:25, manifest_v2.py:22, verify_glb.py:86). gpu_lock.sh gets it right. Same one-liner in all four.

9. **carry — imp_diag_ref.py:196-199: `foliage_p80` is a fixed quantile, not a sky test.** It always discards the brightest 20 % of the box: with more sky than that the ground truth keeps sky (biased blue), with none it throws away the sunlit leaves (biased dark). Threshold on hue or absolute luminance and report the rejected fraction. The 147 deg gap dwarfs this bias, so the verdict is unaffected.

10. **carry — three small ones.** (a) imp_diag_sheet.py:70-82 says the crop is "re-derived here so the sheet and the numbers cannot drift apart" but drops `shift_x`/`shift_y` and uses a wider box than imp_diag_ref — harmless only because station 2's shifts are 0. (b) "Sun-facing frames are green (41-72 deg)" (README #33) has no script that selects frames by sun direction; 41.4 deg is the lowest *nearest-cam02* frame hue in impostor_diag_atlas.json — reword or add the sweep (octa_dir is already there). (c) trees_far/instance_irradiance.json reuses schema id `pfa-phase6/gate4-instance-irradiance/1` while adding a `prototypes` block — bump to /2 so manifest_v4.py:53 can tell them apart.
