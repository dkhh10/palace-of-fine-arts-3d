# phase6-bake round 2 (b99dec1..3f2dc91) — MERGE WITH FIXES

Verdict: **MERGE WITH FIXES** — the 36-job run is real and its numbers replay from the committed scripts; fix 1-4 before the viewer consumes `instance_irradiance.json`, and 1 before `env_trees.glb` ships.

Verified good: round-1 fixes 1-7 are closed (1 only partially, see finding 3). `36 jobs / 764 s` is exactly the sum of the recorded `wall_s` in MAIN's status.json (16 AO 44 s, 16 E_bake 158 s, 4 irr 562 s), and MAIN's status.json ends `idle`, pid/expires_at null. AO mean 0.193-0.502 and calibration 1.0000 in all 16 jobs replay from the shipped JSON; vertex counts really are asserted against topology.json (trees_far_compose.py:83-85), AO asserted in [0,1], cal asserted to 0.002. 127 placements, 0 dark, per-mesh closest pair 6.11 m against a 0.02 m join tolerance. The E_bake job isolates exactly the keep-set the atlas job used (`{proto, GATE3_imp_lawn}`, bake_lm.py:576-580 vs :319-327) and the shadow-ray override is wrapped over the whole `group` scope, so no value depends on the job split; both sides use `mean_nonzero` and ship `cov`, and the spec block at README:1068 now says LOD1-body, `mean_nonzero`, raw/raw. Only `export/` and `renders/logs/` are touched, no tracked binaries, the new scripts all use `PFA_MAIN_ROOT`. `export/README.md` is shared with phase6-export (its items 43-44) — merge bake first or expect a conflict.

1. **fix now — the anchor rule now disagrees with the export, and README item 37 is stale.** `export/trees_far.py` on phase6-export HEAD (e26d8d3, fixed in f95f316) *does* subtract an anchor: `:301` `anchor = (bbox XY centre, z=0)`, `:335` `me.transform(Translation(-anchor))`, asserted against the manifest's `radius_m`/`base_z_m` as the point the impostor rotates about. `trees_far_set.py:163-165` uses a different anchor — the prototype object's own world translation. The two differ by up to (3.44, 2.66) m (pine_s7, pine_s29; >1 m on six prototypes), so E_placement was baked with trees up to ~2.2 m (× s) from where `env_trees.glb` will draw them, and README:1786 "the bake's numbers are correct whatever `env_trees.glb` currently does" is no longer true. Fix: the lead picks one anchor (the export's is the one asserted against the impostor axis); if it is the export's, re-run the four `tfirr_*` jobs (562 s) and restate item 37 as closed-by-export.

2. **fix now — trees_far_set.py:168-174's anchor assert is vacuous.** `base = matrix_world @ (a.x, a.y, 0)` is an algebraic identity for `T(loc) @ S @ T(-a)`; `worst_anchor_residual_m = 0` proves nothing about where the vertices actually land. Fix: assert on the appended mesh's bbox (its XY centre / base z in world space after placement), which would have caught finding 1.

3. **fix now — `gpu_lock.sh check` reads the wrong file, so readers do not agree.** `STATUS="$ROOT/export/out/bake_queue/status.json"` (:29-30) is the *worktree's* copy; CLAUDE.md's signal is MAIN's. `.claude/worktrees/phase6-export/export/out/bake_queue/status.json` is a 2026-09-15 `idle` record, so the export/viewer engineer running `check` there is told the GPU is free during a live bake. Fix: `lock_state` reads `$MAIN/export/out/bake_queue/status.json` (or both, effective-running wins); writes already mirror to MAIN correctly.

4. **fix now — `proto` is missing from `BAKE_DIFFUSE_WORLD_KINDS` (gate3_common.py:88).** Both runs were unarmed (`PFA_BAKE_DIFFUSE_WORLD=<unset>` in the log headers), so the shipped numbers are consistent. But if the lead ever arms it, `instance` (the numerator) bakes against the diffuse-branch sky while `proto` (the divisor) keeps the split-ray world, and every ratio is silently wrong in exactly the channel this whole exercise is about. Fix: add `"proto"`, or have `trees_far_compose.py` assert `rec["rig"]["world"]` equal on both sides.

5. **fix now — "`loose_verts` = 0 everywhere" is false, in README:1806 and in the shipped JSON (`trees_far_compose.py:233-235`).** 11 of the 16 meshes have loose vertices (willow_s11 316, willow_s37 296, eucalyptus_s23 82) and `zeros_loose == loose_verts` in every one, i.e. those zeros *are* unbaked vertices, not crown interior — 316 of willow_s11's 416 zeros. Harmless for shading (a loose vertex is in no face) but it biases `min`/`mean`/`p05`/`zeros_pct` low. Fix: drop loose vertices from the statistics and reword both claims.

6. **fix now — do not let `ratio_check`'s "PASS" stand alone.** The verdict is computed on display B/G, where the full ratio takes 1.2668 past the target 0.648 to 0.0409 (display blue 1.2/255) and passes only because `distance_after` 0.6071 ≤ `gap` 0.6188 — a 2 % margin on a ratio of a near-black channel. The 86.7 % headline is HUE, a different metric from the one the verdict uses, and `instance_irradiance.json`'s `ratio.use` still instructs the viewer to apply the **full** ratio. Fix: the lead records full-strength vs `k = 0.4386` in docs/decisions.md and the `ratio` block states the chosen strength before the viewer reads the file.

7. **carry — the LUT shaper constants are a hard-coded copy.** `trees_far_ratio_check.py:47-48` duplicates `LUT_EV / -12.47393 / 16.5 / 0.18`; `load_cube` skips the `#` header that documents them. They match `gate0_common.SHAPER_*` today, so the display path really is the delivery LUT — but a re-bake at another exposure would silently mis-judge. Fix: parse the header or import `g0.SHAPER_*`/`g0.EXPOSURE_EV`.

8. **carry — `trees_far_compose.py:111-116` does not fail on a missing E_bake.** A half-finished queue would ship a schema /2 file with an incomplete `prototypes` block and only a printed warning; the viewer would have no divisor for those prototypes. All 16 are present today. Fix: assert `not missing_eb`, or refuse to write the file.

9. **carry — two prototypes' LOD2 meshes float.** `topology.json` has `bbox_min.z` 3.6541 (cypress_column_s2) and 4.3547 (redwood_s13) where the other 14 are ~0, so those crowns sit 2.4-2.8 m (× s) above `trunk_base` in `trees_far_irr.blend` and in the glb. The export's reduction, not the bake's — hand it to the export engineer with finding 1.

10. **carry — the s53 determinism check survives only in the logs** (`6c_bake_run1.log:291` and `6c_bake_run2.log:96`, both `mean=3.4148 cov=0.943`); run 2 overwrote `out/gate3/bake/tfeb_*.json`. Adequate evidence, but it is round-1 finding 3 in a new place: keep the superseded record beside the new one.
