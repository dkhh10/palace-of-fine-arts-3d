# Phase 6b Gate 5 — export hand-off (tiers, groups, mobile)

Generated by `export/gate5_report.py` from `export/out/gate5/*.json`; every number is the one in the manifests, read back, not a second computation. Branch `phase6b-export`.

## Tier bytes

| variant | tier | files | bytes | MB |
|---|---|---|---|---|
| desktop | 0 | 219 | 48,214,316 | 48.2 |
| desktop | 1 | 288 | 490,376,099 | 490.4 |
| desktop | 2 | 50 | 58,089,295 | 58.1 |
| desktop | **all** | 557 | 596,679,710 | **596.7** |
| mobile | 0 | 235 | 46,461,150 | 46.5 |
| mobile | 1 | 58 | 5,556,749 | 5.6 |
| mobile | 2 | 48 | 9,415,204 | 9.4 |
| mobile | **all** | 341 | 61,433,103 | **61.4** |

## The first frame, on the wire

The budget is **transfer bytes as the network log sees them**: `gzip -9` for the types Cloudflare Pages compresses (html, css, js, json, txt, svg), size on disk for KTX2, glb, wasm, `.hdr` and `.cube` (Pages serves those as `application/octet-stream` and does not compress them). gzip, not brotli, so the estimate errs high.

| variant | tier 0 transfer | boot overhead | response headers | ON THE WIRE | target | verdict |
|---|---|---|---|---|---|---|
| desktop | 48,186,411 | 1,103,153 | 36,450 (225 x 162) | **49,326,014** | 49,500,000 | WITHIN |
| mobile | 46,433,245 | 1,094,263 | 39,042 (241 x 162) | **47,566,550** | 49,500,000 | WITHIN |

The header allowance is measured, not assumed: Chrome's `encodedDataLength` is body + response headers, and on the 14 tier-0 responses no host compresses (`.hdr`, `.glb`, `.cube`, `.wasm`) the body size on disk is known exactly, so the difference IS the header block - **162 B**, median, from the viewer's own log. The rest of the gap between this table and the viewer's 50.4 MB dev-server measurement is compression the dev server does not do and Cloudflare Pages does: `manifest.json` 2,517,666 B there against 224,918 gzipped, the Vite bundle 1,177,647 against 321,602, `uv2_relay_status.json` 31,977 against 3,914 - 3.2 MB of text that arrives compressed in production.


`boot_overhead_bytes` is what the browser fetches before frame 1 that is not in `files`:

| item | path | bytes | transfer |
|---|---|---|---|
| page | `web/dist/index.html` | 1,390 | 684 |
| bundle | `web/dist/assets/index-D1fAqVas.js` | 1,177,846 | 331,508 |
| transcoder_js | `web/dist/basis/basis_transcoder.js` | 57,529 | 15,063 |
| transcoder_wasm | `web/dist/basis/basis_transcoder.wasm` | 527,333 | 527,333 |
| manifest | `manifest.json` | 2,570,932 | 228,565 |

To fit, **31 Gate 2 placeholder maps (1,735,243 transfer bytes) moved from tier 0 to tier 1**, least hero-visible first. Their materials' share of the hero frame, from `visibility.json`:

| texture | hero fraction of the frame | transfer |
|---|---|---|
| `gate2_ENV__ENV_ground_colonnade_walk_albedo` | 0.000009 (0.0009 %) | 162,754 |
| `gate2_ENV__ENV_ground_colonnade_walk_roughness` | 0.000009 (0.0009 %) | 47,703 |
| `gate2_ENV__ENV_ground_colonnade_walk_normal` | 0.000009 (0.0009 %) | 28,986 |
| `gate2_ORN__ORN_rosette_ceiling_v1_LOD0_a_albedo` | 0.000013 (0.0013 %) | 55,002 |
| `gate2_ORN__ORN_rosette_ceiling_v3_LOD0_a_albedo` | 0.000013 (0.0013 %) | 53,365 |
| `gate2_ORN__ORN_rosette_ceiling_v1_LOD0_a_roughness` | 0.000013 (0.0013 %) | 45,499 |
| `gate2_ORN__ORN_rosette_ceiling_v3_LOD0_a_roughness` | 0.000013 (0.0013 %) | 45,063 |
| `gate2_ORN__ORN_rosette_ceiling_v1_LOD0_a_normal` | 0.000013 (0.0013 %) | 36,418 |
| `gate2_ORN__ORN_rosette_ceiling_v3_LOD0_a_normal` | 0.000013 (0.0013 %) | 35,808 |
| `gate2_ORN__ORN_frieze_rinceau_v3_LOD0_a_albedo` | 0.000022 (0.0022 %) | 73,967 |
| `gate2_ORN__ORN_frieze_rinceau_v3_LOD0_a_normal` | 0.000022 (0.0022 %) | 58,598 |
| `gate2_ORN__ORN_frieze_rinceau_v3_LOD0_a_roughness` | 0.000022 (0.0022 %) | 25,811 |
| `gate2_ORN__ORN_keystone_v1_LOD0_a_albedo` | 0.000030 (0.0030 %) | 56,806 |
| `gate2_ORN__ORN_keystone_v1_LOD0_a_roughness` | 0.000030 (0.0030 %) | 47,016 |
| `gate2_ORN__ORN_keystone_v1_LOD0_a_normal` | 0.000030 (0.0030 %) | 37,685 |
| `gate2_ORN__ORN_frieze_rinceau_return_v1_LOD0_a_albedo` | 0.000043 (0.0043 %) | 78,842 |
| `gate2_ORN__ORN_frieze_rinceau_return_v1_LOD0_a_normal` | 0.000043 (0.0043 %) | 63,060 |
| `gate2_ORN__ORN_frieze_rinceau_return_v1_LOD0_a_roughness` | 0.000043 (0.0043 %) | 26,301 |
| `gate2_ORN__ORN_keystone_v2_LOD0_a_albedo` | 0.000048 (0.0048 %) | 56,117 |
| `gate2_ORN__ORN_keystone_v2_LOD0_a_roughness` | 0.000048 (0.0048 %) | 46,144 |
| `gate2_ORN__ORN_keystone_v2_LOD0_a_normal` | 0.000048 (0.0048 %) | 37,402 |
| `gate2_ORN__ORN_keystone_v3_LOD0_a_albedo` | 0.000061 (0.0061 %) | 54,852 |
| `gate2_ORN__ORN_keystone_v3_LOD0_a_roughness` | 0.000061 (0.0061 %) | 46,567 |
| `gate2_ORN__ORN_keystone_v3_LOD0_a_normal` | 0.000061 (0.0061 %) | 37,679 |
| `gate2_ORN__ORN_frieze_rinceau_return_v3_LOD0_a_albedo` | 0.000086 (0.0086 %) | 67,638 |
| `gate2_ORN__ORN_frieze_rinceau_return_v3_LOD0_a_normal` | 0.000086 (0.0086 %) | 53,338 |
| `gate2_ORN__ORN_frieze_rinceau_return_v3_LOD0_a_roughness` | 0.000086 (0.0086 %) | 23,861 |
| `gate2_ARCH_rotunda__concrete_podium_albedo` | 0.000087 (0.0087 %) | 117,968 |
| `gate2_ARCH_rotunda__concrete_podium_roughness` | 0.000087 (0.0087 %) | 36,392 |
| `gate2_ARCH_rotunda__concrete_podium_normal` | 0.000087 (0.0087 %) | 2,097 |
| `gate2_ORN__ORN_capital_inner_v1_LOD0_a_albedo` | 0.000190 (0.0190 %) | 176,504 |

The largest of them covers 0.0190 % of the hero frame, and **all 28 are DROPPED from tier 0, not moved into tier 1** - their full-resolution file is already there, so shipping the half-resolution copy as well would be 1,735,243 wasted bytes. Their materials carry no map at all until tier 1: the glb's own base colour stands in.

Before any of them moved, the cheaper lever was spent: the **46 tier-0 normal maps re-encoded at ETC1S qlevel 32 instead of 128**, measured over the whole shipped set in `lowres.json`: **3,057,292 -> 2,520,098 B, -17.6 %** (537,194 B). The only error measurement for that change is the five-file probe in `lowres.json.probe` (qlevel 128 at the same resolution); no A/B was run over all 46, so no error claim is made for the shipped set.

**The probe is in tier 0** (lead's decision, review finding 2). Tier 0 ships no lightmap, so the six probe faces (6.29 MB) are the only indirect light in the first frame; without them it is sky-diffuse only, which is darker and flatter than the low-resolution look the user approved, not merely softer.

The same look at Gate 3 was **671.0 MB** over 288 files, with `orn.glb` (154.3 MB) and `env.glb` (38.1 MB) both over the 25 MiB per-file cap.

## What is in each tier

**desktop**

| tier | kind | files | MB |
|---|---|---|---|
| 0 | sky | 3 | 9.35 |
| 0 | gate2_lo | 115 | 8.48 |
| 0 | lut | 1 | 7.42 |
| 0 | glb | 2 | 6.57 |
| 0 | probe | 6 | 6.29 |
| 0 | glb_texture_lo | 42 | 4.05 |
| 0 | glb_group:env | 1 | 2.12 |
| 0 | glb_group:orn | 1 | 1.57 |
| 0 | impostor_lo | 32 | 1.57 |
| 0 | detail_lo | 15 | 0.76 |
| 0 | relay | 1 | 0.03 |
| 1 | gate2 | 146 | 269.22 |
| 1 | glb_texture_full | 52 | 91.74 |
| 1 | lightmap_atlas | 5 | 48.77 |
| 1 | lightmap | 16 | 38.32 |
| 1 | detail | 15 | 23.28 |
| 1 | foliage | 22 | 13.00 |
| 1 | impostor | 32 | 6.05 |
| 2 | gate2 | 35 | 38.58 |
| 2 | glb_lazy | 3 | 11.76 |
| 2 | glb_group:env | 1 | 6.78 |
| 2 | glb_texture_lo | 10 | 0.69 |
| 2 | glb_group:orn | 1 | 0.28 |
| mobile | mobile_only | 149 | 26.24 |

**mobile**

| tier | kind | files | MB |
|---|---|---|---|
| 0 | gate2_half | 146 | 10.21 |
| 0 | sky | 3 | 9.35 |
| 0 | lut | 1 | 7.42 |
| 0 | probe | 6 | 6.29 |
| 0 | glb_texture_lo | 42 | 4.05 |
| 0 | glb_group:arch | 1 | 2.33 |
| 0 | glb_group:env | 1 | 2.12 |
| 0 | glb_group:ground | 1 | 1.96 |
| 0 | impostor_half | 32 | 1.57 |
| 0 | glb_group:orn | 1 | 1.13 |
| 0 | relay | 1 | 0.03 |
| 1 | lightmap_atlas_half | 5 | 2.07 |
| 1 | lightmap_half | 16 | 1.75 |
| 1 | foliage_half | 22 | 0.97 |
| 1 | detail_half | 15 | 0.76 |
| 2 | glb_group:env | 1 | 6.78 |
| 2 | gate2_half | 35 | 1.70 |
| 2 | glb_texture_lo | 10 | 0.69 |
| 2 | glb_group:orn | 1 | 0.18 |
| 2 | glb_group:arch | 1 | 0.08 |

## Per-file cap (Cloudflare Pages, 25 MiB)

- desktop: **0** files over the cap.
- mobile: **0** files over the cap.
- desktop's five biggest published files: `gate3_lmatlas_orn_0_gamma2.ktx2` 16.7 MB, `gate3_lmatlas_arch_inst_1_gamma2.ktx2` 10.3 MB, `gate3_lmatlas_orn_1_gamma2.ktx2` 10.3 MB, `gate3_lmatlas_arch_inst_0_gamma2.ktx2` 10.0 MB, `env_trees_lod1.glb` 7.6 MB

## ETC1S: wall time and size per texture class

| block | files | MB | wall s | encoder |
|---|---|---|---|---|
| tier0 | 146 | 10.75 | 106.2 | `etc1s /2` |
| tier0_orn_ao | 27 | 3.02 | 20.9 | `etc1s /2` |
| groups_and_mobile | 93 | 7.25 | 59.1 | `etc1s /2` |
| tier0_normals_qlevel | 46 | 2.52 | 31.5 | `etc1s /2` |

The choice the brief left open, measured per map (bytes / RMS against the source png in linear light, the decoded image brought back to the source resolution):

| map | src px | ETC1S | ETC1S /2 | ETC1S /4 | UASTC /4 |
|---|---|---|---|---|---|
| arch:albedo `e_north__concrete_colonnade_albedo` | 2048 | 351 kB / 0.0162 | 96 kB / 0.0293 | 32 kB / 0.0401 | 161 kB / 0.0352 |
| arch:roughness `orth__concrete_colonnade_roughness` | 1024 | 89 kB / 0.0164 | 30 kB / 0.0287 | 9 kB / 0.0401 | 39 kB / 0.0371 |
| arch:normal `e_north__concrete_colonnade_normal` | 2048 | 108 kB / 0.0056 | 12 kB / 0.0059 | 3 kB / 0.0063 | 97 kB / 0.0054 |
| backdrop:albedo `e2_ENVBD__backdrop_building_albedo` | 1024 | 128 kB / 0.0108 | 41 kB / 0.0145 | 11 kB / 0.0190 | 64 kB / 0.0142 |
| backdrop:roughness `ENVBD__backdrop_building_roughness` | 1024 | 103 kB / 0.0068 | 36 kB / 0.0276 | 10 kB / 0.0312 | 54 kB / 0.0302 |
| backdrop:normal `_ENVBD__backdrop_door_green_normal` | 1024 | 6 kB / 0.0035 | 2 kB / 0.0036 | 1 kB / 0.0036 | 34 kB / 0.0028 |

**Half-resolution ETC1S wins the brief's test** — smaller AND lower RMS than quarter-res UASTC on 5 of the 6 sampled maps, and 8-15x smaller on the two normals for +0.0005 RMS — so that is what tier 0 and the whole mobile set use.

## Mobile resident estimate (ASTC 4x4, 1 byte/texel x 4/3 for mips)

| kind | MB |
|---|---|
| gate2 | 157.6 |
| impostor | 44.7 |
| glb_texture_lo | 32.5 |
| lightmap_atlas | 28.0 |
| lightmap | 26.6 |
| detail | 9.4 |
| foliage | 7.7 |
| **total** | **306** |

Target on the iPhone 16 Pro: **< 700 MB**. Desktop, by the same rule: 1093 MB (manifest v4's own carried figure is 1055 MB, so the rule reproduces).

## Hero-visible assets not in tier 0

- 407 assets are visible from the hero station at the 640x360 ray grid.
- **0** of them are not in tier 0.
- 0 `ENV_treeboard_*` stand-ins are deliberately not in tier 0: the viewer hides them by default (`?treeboards=0` since 6a, web/src/main.js) and draws those trees from the impostor atlases, which ARE in tier 0 at half resolution. Their pixels are covered.

## Verification (`export/verify_glb.py --gate5`)

| variant | assets | files | arch | orn | env | ground | result |
|---|---|---|---|---|---|---|---|
| desktop | 2540/2540 | 706 | 0.9981 | 0.9984 | 1.0 | 1.0 | PASS |
| mobile | 2540/2540 | 341 | 0.4723 | 0.6204 | 1.0 | 1.0 | PASS |

The ratio is triangles DRAWN against `glb.per_class[cls].placed_tris` at Gate 3. Desktop's 0.9981 / 0.9984 on arch and orn are the same degenerate-triangle loss Gate 3 itself reported (-0.185 % / -0.157 %); env and ground are exact. The mobile ratios are `gltfpack -si 0.5` on ARCH and ORN, which is the brief's "decimated LOD0 at half the Gate 1 budget" - the export set has no ARCH/ORN LOD1 to ship instead.

## Carried, not fixed (for the next round)

- `../gate1/arch.glb` (15 images) and `../gate1/ground.glb` (4) still EMBED their Gate 1 stand-in maps in buffer views. They are not groups, so the no-embedded-image assertion skips them, and those maps can never be tier-upgraded - harmless today because `web/src/pbr.js` overwrites every one of them from the Gate 2 set, and both files are small (4.6 / 2.0 MB), but it is the same dead-end the groups were fixed for.
- `scene.ray_cast` treats alpha-cut leaf cards as opaque, so the tier ordering is conservative behind foliage: an asset hidden only by a leaf card reads as invisible and lands a tier late.
- `verify_glb --gate5` compares drawn triangles per class but does not compare instanced-ROW counts against a Gate 3 figure; the placements column is reported, not asserted.
- `tiers.py` still prints two hardcoded byte figures for `orn.glb` / `env.glb` in the `glb.note` text (154 253 424 / 38 119 568) that differ from `per_class_gate3` (154 065 360 / 35 797 240): the note quotes the files on disk, the block quotes what manifest v4 recorded at Gate 1.
- `gate5_tex.py --mobile` no longer writes a second texture set; it now says so and exits.
- `lowres.json` `wall_s` is this run's seconds; `wall_s_cumulative` is the running total.

## Viewer contract — what changed in v5 (breaking, on purpose)

- **`glb.per_class` is gone**, replaced by `glb.groups` (a list with `path`, `cls`, `tier`, `nodes`, `assets`, `hero_fraction`, `station_visibility`, `textures`, `placeholder`). The Gate 3 figures are kept as `glb.per_class_gate3` for reference only: `orn.glb` (154.3 MB) and `env.glb` (38.1 MB) are NOT published, because both are over the 25 MiB per-file cap. A viewer that still reads `per_class` will 404 rather than quietly download 192 MB of files that are not on the host.
- **`files`** is the load plan: an array already in (tier, hero coverage, path) order, each entry `{path, tier, kind, why, bytes, key, px}`. `tiers.bytes` is the loading-screen denominator.
- **`tiers.lowres.files[key]`** gives the tier-0 half-resolution variant of a texture key; the viewer uses it in tier 0 and re-loads the manifest's own full-resolution path when tier 1 lands.
- A group with **`placeholder: true`** is tier 0 and is replaced by the tier-1 group of the same class; its assets are asserted to be a subset of that group's.
- **149 rows in this plan carry `kind: "mobile_only"`, `tier: "mobile"` and `desktop: false`** (26,241,681 B: the 7 `groups/m_*.glb` and the half-resolution files only the mobile plan fetches). **A desktop tier loop over 0, 1, 2 must skip them** - the tier is a STRING, so a numeric comparison already does, and `desktop: false` is there for a filter. A deploy must publish them: that is the point of naming them here.
- **`?tier=mobile`** selects `manifest_mobile.json`, which carries the same blocks. Its `lowres.files` entries have **no `full`**: the mobile set publishes only the half-resolution copy, so its redirect is permanent and there is nothing to upgrade to.
- **31 keys listed in `tiers.tier0_trim` have NO tier-0 map** - no entry in `tiers.lowres.files` either - and their materials show the glb's own base colour until their full-resolution file arrives in tier 1. "0 hero-visible assets outside tier 0" is about GEOMETRY; these 28 maps are the texture exception, each under 0.003 % of the hero frame.
- **There are no placeholder groups any more.** Every instance of a prototype is in ONE group, so the hero draws the Gate 3 node set exactly; what tier 1 upgrades is the TEXTURE. For a key in `tiers.lowres.files`, `path` is the half-resolution file the group or the material already has and `full` is the full-resolution file to re-load into the same slot - including the ORN occlusion maps, which arrive inside the glb at half resolution and are named by each set's `occlusion.gate1_texture`.
- **`uv2_relay_status.json` is published beside the manifest** (tier 0) and named at `lightmaps.uv2_relay_status.path`; main.js already resolves it against the manifest URL.
- **`lightmaps.instance_irradiance.groups`** replaces the env.glb-keyed `nodes` (see above).
- **`files[*].transfer`** is the gzip-estimated wire size; `tiers.transfer_bytes` and `tiers.first_frame_transfer_bytes` are what the loading screen should count against.
- The four v4 paths that were relative to `out/gate3` are rebased to `../gate3/...`; what moved is listed in `tiers.path_rebase`.

## The water

- none: the lagoon ripple is procedural (8 analytic waves in web/src/water.js, `rippleTiling`), so there is no ripple or normal map to tier - 0 bytes in every tier. The tier-0 water differs from the tier-1 water only in what it REFLECTS, which is the scene, not a map of its own.

## ENV instances after the split

- ENV draws 1 523 instanced rows + 6 plain nodes = 1 529 placements against env.glb's 1 526 + 5 = 1 531. Nothing is missing: all 1 536 source nodes are in the groups and the triangles DRAWN are 679 779, exactly Gate 3's. gltfpack merges single-use meshes that share a material, and it merges two more of them in the split than in the whole file, so three cards that were instanced rows are now inside a merged plain node. Those three are the `.001` near-duplicates listed in `instance_order_groups.json.unmatched`: a merged plain node has no per-instance row, so their irradiance cannot be keyed and they fall back to the probe, as the seven fully enclosed cards already do.

## The deploy set — every directory the publish must carry

`files` spans four gates, not just gate5: the LUT and the sky are gate0, the glb-external maps and the lazy foliage glbs are gate1, the Gate 2 PBR and detail sets are gate2, the lightmaps, impostors, probe and foliage cards are gate3. **A deploy that publishes only `out/gate5` 404s on every `../gate0/...` path.**

| directory | files | bytes | transfer |
|---|---|---|---|
| `export/out/gate2` | 196 | 331,082,777 | 331,082,777 |
| `export/out/gate3` | 82 | 114,531,520 | 114,531,520 |
| `export/out/gate1` | 57 | 110,071,435 | 110,071,435 |
| `export/out/gate5` | 368 | 52,568,697 | 52,540,792 |
| `export/out/gate0` | 3 | 14,666,962 | 14,666,962 |
| **all** | 706 | **622,921,391** | 622,893,486 |

**The deploy set is the union of both manifests' `files[]`, and after the QA-18 fix that union equals `manifest.json` alone**: 149 rows of it are the mobile-only files, `kind: mobile_only`. Publishing from `manifest.json` is complete; unioning both plans is equivalent and also correct. What must NOT happen is publishing from `manifest_mobile.json` alone - it is a LOAD plan whose material sets name the full-resolution keys the viewer redirects, so a walker resolving them there would publish the whole desktop set.


The mobile set is a subset of the same directories. `web/deploy.sh` should build the publish directory FROM `files` (copy each path, keeping it relative to `out/gate5`), not by copying `out/` wholesale: `out/` also holds ~2 GB of bake sources, the `rgbm8` lightmap twins and the unpublished `orn.glb` / `env.glb`, none of which are in the plan.

## Shrub and reed irradiance after the split

- `lightmaps.instance_irradiance.groups` re-keys the block to the per-tier ENV groups: 1376 of 1379 placements, INCOMPLETE.
- **How the viewer joins it:** for each group id in `groups`, bind under `WEB_glb_<group id>` by `mesh.userData.pfaGltfNode`, exactly as `applyInstanceIrradiance` does today, passing that group's scene as `opts.root` and `groups[id].nodes` as `opts.block.nodes`. `segments` has the same `[mesh, count, offset]` meaning; `meshes[*].rgb` is unchanged.
- The join is the bake's own key, the instance translation: rows decoded from the packed group with `web/tools/instance_rows.mjs`, brought back to Blender space, matched to the nearest placement within 0.02 m with the runner-up at least 3.0x further; a placement claimed twice is a hard failure.
- **3 placements are unmatched** and fall back to the probe: `ENV_shrub_maho2_0281_LOD2`, `ENV_shrub_pitto5_0282_LOD2`, `ENV_shrub_reed1_0283_LOD2`. These are the three `.001` near-duplicate cards the manifest's own `lod1` block already reports as `not_in_lod1: 3`.

## Files in MAIN

- desktop: 706 published files, **0 missing** from `/Users/dk/Projects/3d render blender 3rd attempt building/export/out/gate5`.
- mobile: 341 published files, **0 missing** from `/Users/dk/Projects/3d render blender 3rd attempt building/export/out/gate5`.

