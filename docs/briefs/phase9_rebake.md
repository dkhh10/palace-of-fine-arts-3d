# Phase 9 re-bake chain — after lighting r19 (brief from the lead, 2026-09-21). Opus 5 xhigh (bake engineer). Fresh agent. You own the GPU.
Read docs/briefs/process.md, CLAUDE.md (Phase 6 machine rules: bake_queue.sh owns the GPU while status.json says running; one Blender per asset through
scripts/blender_run.sh with honest seconds; never a Chrome beside you), then docs/briefs/phase9_bake_analysis_report.md Part A in full (A.0-A.4: the priced chain,
the two-job probe, resume, the QA-23 far-tree rule), docs/briefs/phase9_light_report.md (hand-off: diffuse sockets only -> camera/glossy equirects and the LUT
are NOT re-baked; only lightmaps, the instance irradiance, the hero probe, sky.diffuse equirect), docs/reviews/phase9_bake_r1_review.md carries, export/README.md
"The chain the lead runs" (Phase 9 export section: trees_far.py far set FIRST, before any trees_far_set.py bake — a leftover 149-row topology.json hard-fails),
docs/status.md from "SESSION 10 START".
Branch `phase9-rebake`, worktree .claude/worktrees/phase9-rebake for scripts/docs; the bakes themselves write MAIN's export/out (that is the bake source of truth,
regenerable); MAIN's master_delivery.blend was regenerated by the lead with the r19 lighting (renders/logs/lead_build_r12.log, phase5_1b_r12.log; marker
renders/logs/.p9_master_rebuilt). Never edit it. Before every Blender launch: `pgrep -fl "MacOS/Blender|headless"` empty.

## Order
0. Six Phase 9 Cycles AFTER refs for QA parity: scripts/p8_cycles_refs.py on a scratch copy of the new master_delivery, all six stations, 1080p 32 spp, into MAIN
   renders/qa_comparisons/cycles_p9/cam0N_1080_32spp.png (+ 960 px copies committed under cycles_p9/960/). The lighting agent's r19_ship_0{1..4} are the same
   recipe for 1-4; render all six anyway (one recipe, one file set). Marker renders/qa_comparisons/cycles_p9/.p9_refs_done.
1. The A.2 probe (316 s): the two cheapest own maps + slot_arch_inst_1_06 re-baked on the new master_delivery, compared texel-wise against the shipped bake
   (MAE / max / per-decile, lit vs shaded texels). Verdict per A.2: slots re-bake or not. Log the numbers in docs/briefs/phase9_rebake_report.md before continuing.
2. The chain per the verdict (reduced 44 jobs ~3 h 39 m, or full 87 ~7 h 24 m) through export/bake_queue.sh with the manifest of jobs written first; status.json
   is the lock; resume on failure per A.4; the QA-23 far-tree rule (the 127 existing rows' irradiance restore / p8d_irr_restore.py retirement question in A.4 —
   decide and log). trees_far.py (far set) BEFORE trees_far_set.py. Every bake round reports min / max / clipped-pixel count per asset (CLAUDE.md Phase 6).
3. After the queue: the export/pack chain from export/README.md "The chain the lead runs" (Phase 9 section) up to and including `tiers.py` x3, `verify_glb --gate5`,
   `tiers_test`, `name_sweep`, `npm run build` — then STOP before deploy and report. The lead grants the viewer its capture window and deploys.
Commit after every completed step (scripts, manifests' JSON schemas, the report; never the EXR/KTX2/GLB), suite counts in every message ("no suite" where none),
messages end "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>". If a usage limit hits, stop; the queue resumes from status.json.
## Report (< 25 lines): refs done; probe table + verdict; chain jobs / wall time / failures / resumes; per-asset min/max/clipped summary (worst 5); the pack chain's
results (tier sizes, verify_glb, name_sweep); last commit id; what is left for the lead (deploy command).
