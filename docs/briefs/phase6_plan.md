# Phase 6 plan — Three.js walkthrough (lead, 2026-09-15)

Source of every number: `export/inventory.py` run on master_delivery.blend (regenerated by the user 2026-09-15 09:37 from
the a765b9d master: 9691 objects, 89 packed images, 280 MB), output `export/inventory.json` (gitignored, 29 s to rebuild).

## 1. What the scene actually contains (placed triangles, modifiers applied)

| class | objects | unique meshes | placed tris | note |
|---|---|---|---|---|
| ARCH_ LOD0 | 149 | 17 | 2 029 416 | 138 fluted columns at 14 396 tris each = 1.99 M; eggs 30 720; dentils/modillions/mutules |
| ARCH_ single-LOD | 1 032 | 629 | 818 418 | column bases 224 x ~1.4 k, astragals, rostra, attic parts, piers, vault plates |
| ORN_ via INST_ LOD0 | 436 | 33 | 22 915 791 | capitals 114 x 48 k, drum bands 105 x 30 k, maidens 48 x 100 k, urns 40 x 60 k ... |
| ENV_ LOD1 | 1 526 | 53 | 4 492 726 | trees 3.33 M (cypress 812 k, eucalyptus 634 k, pine 525 k, willow 443 k, redwood 410 k), shrubs 1.16 M |
| ENV_ single-LOD | 1 347 | 1 347 | 282 146 | backdrop city blocks, Presidio, riprap, canopy, lagoon bed, ground |
| ENV_ LOD2 (alternative) | 1 526 | 41 | 388 276 | |
| PH_ / ORN_ prototypes | 388 / 138 | | 0 | zero-triangle placeholders (name-sweep exempt on record) and origin-parked prototypes: never exported |
| **user's selection as stated** | | | **30.5 M** | **10x the 3 M budget; decimation is the whole of Gate 1** |

Materials: 72, all Principled; every stone material is the same three 2K image sets (concrete_wall_007 / 008 diff, rough,
disp) driven through procedural mixes (the round-9 photo projection, per-instance seeds, tint sockets), so a PBR bake is
required to flatten them. Foliage: 1K diff/normal/translucency card textures (bake-free: reuse as is). World:
`WORLD_golden_hour`, MULTIPLE_SCATTERING sky (elevation 7.36 deg, rotation 28.5 deg = azimuth 118.5), no sun disc, with
light-path branches (camera x2.5, glossy x1.7, diffuse tinted sun-side/anti-sun/horizon). Lights in Cycles: LIGHT_sun
67.3 W/m2, three rotunda bounce area lights; 16 gallery strips are Eevee-only (0 W in Cycles, hidden at render).
View: AgX, look `AgX - High Contrast`, exposure -2.833, plus the compositor (mist airlight cap 0.25 / k 5.0, bloom,
vignette) on every Phase 5 frame.

## 2. Budgets (Gate 1 freeze; per-asset table lands in docs/briefs/phase6_budget.md from the export engineer)

Total <= 3.0 M placed triangles, <= 400 draw calls at the hero station, texture memory <= 1.2 GB resident on 6a.

| class | placed budget | how |
|---|---|---|
| ARCH_ | 1.10 M | columns 14.4 k -> 3.5 k (flutes into the normal map; 138 x 3.5 k = 0.48 M); bases 1.4 k -> 0.4 k; eggs 30 k -> 8 k; everything else as modelled (~0.5 M). Columns, bases, astragals stay instanced (shared mesh, EXT_mesh_gpu_instancing). |
| ORN_ | 1.10 M | per prototype (33 meshes) decimate LOD0 to 2-8 k tris with a hi->lo normal + AO bake: capital_colonnade 2.5 k, drum_band 1.5 k, maiden 5 k, urn 2.5 k, keystone 1.5 k, rosette 1.5 k, capital_rotunda 6 k, attic_figure 8 k, attic_panel 8 k, inner_figure 6 k, frieze_run 3 k, finial 2 k, capital_inner 3 k = 1.24 M placed; trim the two largest classes if over. |
| ENV_ | 0.80 M | trees inside the hero frustum or within 80 m of any station at LOD1 (leaf cards merged, decimated 50 %), all other trees and every shrub at LOD2; backdrop/riprap/ground as modelled (0.28 M). |
| ground / water | in ENV | lawn, paths, lagoon bed as modelled; the water is a viewer plane at WATER_Z, not exported geometry. |

Textures (KTX2 UASTC on desktop, ETC1S on mobile; ASTC 4x4 on Apple GPUs = 8 bpp, x1.33 for mips):

| set | count | size | resident (est.) |
|---|---|---|---|
| ARCH PBR (albedo, roughness, normal) | ~30 atlases (objects grouped per element and material) | 2K, 4K for the 6 assets inside the hero frame within 30 m of cam01 | 30 x 16 MB + 6 x 48 MB = 0.77 GB (6a); mobile halves = 0.19 GB |
| ORN per prototype (normal, AO, albedo) | 33 x 3 | 2K (1K mobile) | 33 x 16 MB = 0.53 GB; trimmed to 1K for the small classes -> ~0.3 GB |
| lightmaps UV2 | ~40 ARCH/ENV assets | 2K RGBM8 in KTX2 (HDR kept as EXR only on disk) | 40 x 5.3 MB = 0.21 GB |
| foliage cards | 5 species x 3 | 1K as shipped | 20 MB |
| sky | 2 equirects 4K x 2K EXR -> HDR (RGBE) | background + PMREM source | 40 MB |
| LUT | 33^3 | | < 1 MB |

Rounded: 6a resident ~1.1-1.3 GB (unified memory, fine on 24 GB); 6b desktop download ~350 MB total, initial payload
50 MB = geometry (meshopt ~5 B/tri = 15 MB) + LUT + sky + the hero-frame assets' 1K mips, the rest streamed by distance.
Mobile fallback ~120 MB total (LOD1/LOD2 geometry, halved textures, ETC1S).

## 3. Lighting transfer (what the viewer gets from Phase 5, and the traps)
- Lightmap = Cycles Diffuse pass, direct + indirect, no colour, at the final rig (`light_presets.apply_final_cycles`
  so the Eevee-only rigs are off), 128 spp + OIDN, EXR. It already contains the sun's diffuse: the viewer's sun
  DirectionalLight must be specular-only (onBeforeCompile) or it double-lights every surface. No shadow maps.
- Sky: two equirects from the world node tree, the camera branch for the background sphere and the glossy branch for
  PMREM; the diffuse branch is inside the lightmaps and is not exported.
- ORN instances cannot share a lightmap per instance: plan is prototype-level normal + AO (UV1, shared) plus a per-instance
  Cycles vertex-colour bake of the diffuse pass (target VERTEX_COLORS; instances get unique mesh copies at ~1.2 M tris).
  Decided at Gate 3 after the Gate 0 timing; the alternative is a 256-px-per-instance lightmap atlas (4 x 4K atlases).
- Trees: no lightmap; vertex-colour AO bake per tree prototype + the same sky PMREM + sun.
- Colour: 3D LUT baked through Blender's OCIO (scene-linear -> AgX High Contrast at -2.833 EV) and applied in a post
  LUT pass with three.js tone mapping off; bloom and vignette as the compositor's numbers; mist as distance fog whose
  colour and density are fitted on the hero's far-shore boxes.
- Water: three.js planar Reflector on the plane at WATER_Z with the lagoon murk tint and a ripple normal map; the baked
  cubemap probe at the hero station is the fallback.

## 4. Bake times, measured at Gate 0 (bake engineer, 2026-09-15, M2 10-core Metal, Cycles GPU, one Blender at a time)

Slice: `ARCH_rotunda_column_00_LOD0` 14 396 -> 3 500 tris, `INST_capital_rotunda_000_LOD0`
(`ORN_capital_rotunda_v2_LOD0_a`) 64 000 -> 6 000 tris, ground `ARCH_rotunda_pedestal_00` 12 tris as modelled.
All maps 2048^2. Every number below is a wall time printed by the script, not an estimate.

| bake | settings | column | capital | ground | prior estimate |
|---|---|---|---|---|---|
| normal, hi -> lo, tangent | 4 spp, measured cage (4.3 mm / 28.2 mm) | **6.7 s** | **9.3 s** | 9.1 s (own shading normal, no hi twin) | < 60 s: right |
| AO, hi -> lo | 128 spp | **146.2 s** | **220.9 s** | **226.5 s** | < 60 s: **wrong, 2.4-3.8x** |
| albedo (DIFFUSE, colour pass only) | 16 spp | 22.6 s | 29.6 s | 30.8 s | 60-120 s: 2x pessimistic |
| roughness (ROUGHNESS) | 16 spp | 22.6 s | 30.1 s | 31.2 s | (same pair) |
| **lightmap** (DIFFUSE direct+indirect, colour off) | 128 spp + OIDN, UV2 | **305.6 s** | **221.3 s** | **461.0 s** | 5-12 min: right (3.7-7.7 min) |
| lightmap in a 256 px atlas slot | 128 spp + OIDN | - | **5.1 s** | - | - |
| all four maps, per asset | | 503.7 s | 511.2 s | 758.6 s | |

Denoising: Blender 5.2's `BakeSettings` has no `use_denoising`; the bake honours `scene.cycles.use_denoising`,
which is what these lightmaps used. `scene.cycles.use_adaptive_sampling` is off for every bake (fixed spp).

Non-bake steps, same machine: `export_set.py` (open the 280 MB packed master, decimate, two smart-project UV
unwraps, strip 9 653 objects, save the 22.6 MB working .blend) **18.8 s**; `gltf_export.py` **2.7 s**; `toktx`
UASTC q2 + zstd 18 + mips on 9 x 2K PNG **29 s**; `gltfpack -cc` **< 1 s**; LUT lattice through the compositor
**0.5 s** plus five Cycles proof patches **1.0 s**; the two 4096x2048 equirects at 16 spp **5.6 s + 5.7 s**; the
1280x720 / 64 spp Cycles reference frame of the 17-object slice **11.6 s**.

Projection for the Gate 3 queue at the same settings: ~40 ARCH/ENV assets x (lightmap 329 s mean + AO 198 s mean +
PBR pair 54 s + normal 8 s) = **~6.5 h**, every job well under the 1800 s `blender_run.sh` cap, so the queue is
per-asset and resumable. The AO row is the correction to make to the Gate 1 plan: AO is the second most expensive
bake, not a free one, and at 64 spp it would halve.

Lightmap value range (the -2.833 EV question), scene-linear, 2K, 128 spp + OIDN:

| map | min | max | mean | mean of non-zero | p99 | RGBM8 range | clipped px | worst round-trip |
|---|---|---|---|---|---|---|---|---|
| column | 0.0 | 51.77 | 5.77 | 7.83 | 33.30 | 64 | **0** | 0.097 abs, 4.8 % rel p99 |
| capital | 0.0 | 53.72 | 3.50 | 5.05 | 44.22 | 64 | **0** | 0.100 abs, 4.8 % rel p99 |
| ground | 0.0 | 48.69 | 4.15 | 9.09 | 33.00 | 64 | **0** | 0.089 abs, 4.8 % rel p99 |
| capital 256 px | 0.0 | 50.96 | 5.03 | 5.04 | 43.12 | 64 | **0** | 0.098 abs, 4.3 % rel p99 |

RGBM8 with range 64 carries the whole range with nothing clipped; the cost is a 4.8 % relative error at the 99th
percentile, which a gamma-2 encode of the RGB part would roughly halve if a later round needs it.


## 5. Gates
0 vertical slice (docs/briefs/phase6_gate0.md) -> 1 geometry freeze (export set, decimation, ORN normal bakes, name sweep,
silhouette + normal check at the six stations at full-resolution tiles) -> 2 material bake (per asset albedo/rough/normal,
2K, 4K in the hero-near set) -> 3 lightmap bake (UV2, 2K, Cycles diffuse, detached queue with status.json + resume, one
hero-station reflection probe) -> 4 viewer (MeshStandardMaterial + lightMap, PMREM sky, planar water, LUT/bloom/AO post,
loading screen, station presets; QA rounds start) -> 5 web (payload, hosting, mobile, staging, QA on the URL).
Measure the material bake and the lightmap bake separately in the viewer before combining (retrospective lesson).

## 6. Toolchain (installed 2026-09-15, tools/install.sh re-fetches)
gltfpack 1.2 (native, texture compression built in), toktx / ktx 4.4.2 (KTX-Software Darwin-arm64 pkg, extracted to
tools/bin + tools/lib, rpath fixed), node 25.9 / npm 11.12, three 0.186, vite 8.3, Chrome 152 headless WebGL2 on ANGLE
Metal (M2; ASTC, ETC, BPTC, S3TC, float-linear all present), ffmpeg 8.1, Blender 5.2.1 (bpy exporter). Not present:
ImageMagick (use sips / Blender), PIL inside Blender's Python (system python3 has PIL 11.3 + numpy 2.0).
