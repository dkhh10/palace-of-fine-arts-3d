# Phase 9 export — finishing report (branch `phase9-export`, last commit 591539b, 2026-09-20)

**cam03 frustum table** (`python3 export/belt_rule.py --frustum`, computed from the gate3 manifest's own station, lens and crown sizes — it was prose until this round):

| row | tree | d crown (trunk, xy) | in frame | ndc x | ndc y | crown px | px / impostor texel |
|---|---|---|---|---|---|---|---|
| 40 | `ENV_tree_cypress_33_LOD1` | 7.1 (6.5, 5.9) | **YES** | -2.27 .. +0.10 | -1.81 .. 4.42 | 1 435 | **17.7** |
| 147 | `ENV_tree_redwood_26_LOD1` | 8.2 (6.6, 6.1) | no — one corner clears the near plane (+0.2 m), projects to ndc x -27.8 | – | – | – | – |
| 41 | `ENV_tree_cypress_34_LOD1` | 11.8 (10.4, 10.1) | **YES** | -1.74 .. -0.38 | -0.85 .. 3.03 | 956 | **11.8** |

Both over the 8e believable band (9-13 px), so **option (a) stays rejected**; (b) as worded is empty (all 39 belt trees are 0.2-7.3 m from a walkable surface); **chosen (b) re-cut per set**: a tagged row keeps its mesh only within that set's own `draw_within_m` + the 5 m fade band of a QA station eye.

1. **Walk-up (desktop, 20 m): 4 kept / 35 billboard-only, 4 937 933 -> 3 890 782 placed tris (-1 047 151, -21.2 %), rows 166 -> 131** — removes 1 047 151 of cam03's 1 301 870 excess triangles and ~24 of its 28 draws.
2. **Far (mobile, 50 m): 22 kept / 17 billboard-only, 1 317 097 -> 1 182 338 (-134 759, -10.2 %), rows 166 -> 149** — ~19 draws.
3. **NO VIEWER CHANGE REQUIRED — the deploy is not gated on one** *(corrected in fix round r2; the r1 version of this item claimed the opposite and it was wrong — review r1 finding 2)*. `iIrr` is written at **build** time: `main.js` runs `farTreeIrradiance` over **all** `manifest.treesFar`, joined to `trees.far_mesh.lighting` **by location** (never by mesh placement), and `buildImpostors` writes it per instance. A billboard-only row therefore keeps its `E_placement / E_bake` modulation as long as that lighting list keeps its row — which `manifest_v4` guarantees (one row per far tree, `mesh: true|false`) and `verify_glb.far_tree_counts` / `p9_rule_selftest` both fail on. `foliageLazy` builds its impostor complement from the MESH placements, so such a row is correctly never flipped to `iNear = 1` and `activateImpostorMeshes` merely never re-writes a value it already has. **What to expect in the counters:** main.js's `far-tree impostor modulation: N/N placement(s) joined by location` must still read **166 / 166** (below that is a real defect); `activateImpostorMeshes`' `re-lit` count drops from 166 to the loaded set's mesh row count, **131 desktop / 149 mobile** — reporting, not lighting. Contract: `web/test/foliage_lazy_test.mjs` section 4c, whose `info` line describes the harness (it builds impostors without `irr`, which main.js never does), not the viewer.
4. **Pin delta** (`export/p8d_pin.py`): the export set does **not** move (`tree_rule` 186/166/85/20, ENV placed 895 052, arch/orn/ground byte-identical). Six numbers move, all downstream: `trees_far[far|walkup].placements` **149 / 131**, `billboard_only` **17 / 35**, `placed_tris` **1 182 338 / 3 890 782**. 13 pins in all; the subset relation is now checked **by row index**, not by count.
5. **Command list**: verbatim in `export/README.md`, section "The chain the lead runs" (grep `### The chain the lead runs`). Order: the three CPU checks -> `trees_far.py` far then walkup (both, in that order) -> `gltf_pack.sh --trees` / `--trees-lod1` -> `p8d_pin.py` -> `instance_rows.mjs` x2 -> gate4 order + selftest + `verify_glb` -> `manifest_v2/v3/v4` -> `budget_doc` -> `sync_main` + `tiers` x3 -> `verify_glb --gate5` desktop+mobile -> `npm test` + `tiers_test` + `name_sweep` -> `p8d_pin --glbs` (after the pack, never before) -> `sync_main`. The manifest grows **+6 166 B gzipped** (0.8 % of the last deploy's headroom); nothing else grows.

**Done this round** (each its own commit, suite counts in every message): merged main (both 2026-09-20 decisions entries kept); `belt_rule.py --frustum` — the table above became a script, with the invariant *no belt row in a station's frame may be billboard-only in either set*; carry r1 item 4's second half (a `glb.per_class` class that is **not** refreshed now says so, both directions); the pin's subset check by index. Two prose corrections the script forced: px/texel is 17.7 / 11.8 (not 17.9 / 11.9 — rounding), and "d to eye" was the crown-centre distance while the rule measures the trunk base (both now labelled; a centre-point frustum test calls cypress_33 invisible and would have justified option (a)).

**Carries audited as done**: r1 item 2 (`impostors.variant_2k.tier0_stand_in`), r1 item 3 (`PFA_MAIN_ROOT` default, CAM03 read from the manifest, `p8a_shrub_check` exits non-zero), r1 item 4 (`p8c_ktx2_compare.py` + the per_class print), r6 item 7 (a non-`--glbs` run drops the stale `glbs` block and stamps `glbs_note` instead of carrying it).

**Suites, all CPU, all green**: `belt_rule` PASS, `belt_rule --frustum` PASS (2 of 3 in frame), `p9_rule_selftest` **34/34** (16 -> 23 in r1, -> 34 in the r2 fix round, which added the irradiance-topology contract), `gate4_order_selftest` **10/10**, `web npm test` **450/450**, `foliage_lazy` 4c PASS. Not runnable here: `verify_glb`, `manifest_v4.main`, `p8d_pin.main`, `tiers` — all need `export/out` artefacts this worktree does not hold; they are in the chain above. **No Blender, no bake, no pack, no deploy, no Chrome** — nothing needed one: every number came from the gate3 manifest and the two `trees_far*.json` reports.

## Fix round r2 (2026-09-20, against `docs/reviews/phase9_export_r1_review.md`)

* **Finding 1 (blocker) closed.** `topology.json` carries a row for **every** `tree_far` tree again — the billboard-only ones flagged `has_mesh: false` and carrying the same `object`, `scale`, `loc` and independently computed `placed_bbox_*` as a placed row — because `export/trees_far_set.py` builds the per-placement irradiance blend from that list and the bake must cover all 166 (an excluded row's impostor is modulated by it too). The excluded objects are built by the same code and linked into `EXP_TREEFAR_BILLBOARD_ONLY`, which the glTF export does not select, so the glb still ships 149 / 131 rows. `belt_rule.topology_problems` / `check_topology` is the one contract the writer, the reader and `p9_rule_selftest` all call; `p9_rule_selftest` gained 11 checks, the first of which is the blocker itself.
* **Finding 2 (fix now) settled: the reviewer is right — item 3 above is rewritten and no viewer change is required.** Verified against `main`'s own source, read-only: `web/src/main.js:1101-1104` builds `treesFar` with `irr` from `farTreeIrradiance( manifest.treesFar, … )` — the whole list — and passes it to `buildImpostors` at `:1118-1119`; `web/src/foliage.js:1160-1168` builds the join map **by location**, not by mesh placement; `web/src/impostors.js:951-952` writes `iIrr` from `t.irr` at **build** time; `web/src/foliageLazy.js:700` (`placements.find( q => q.billboard === t.id )`) is what skips a billboard-only row, and `impostors.js:1105-1108` only ever **re-writes** the same value, bumping the `modulated` counter. The README section, `decisions.md`'s entry, `trees_far.py`'s note and `viewer_change` field, `manifest_v4`'s docstring and the 4c `info` line all now say so.
* **Findings 3-5 (carries)** stay tabled in `export/README.md`'s carry list: each is more than the 10-line budget this round allowed.
