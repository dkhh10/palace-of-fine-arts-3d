# Phase 9 export — finishing report (branch `phase9-export`, last commit 591539b, 2026-09-20)

**cam03 frustum table** (`python3 export/belt_rule.py --frustum`, computed from the gate3 manifest's own station, lens and crown sizes — it was prose until this round):

| row | tree | d crown (trunk, xy) | in frame | ndc x | ndc y | crown px | px / impostor texel |
|---|---|---|---|---|---|---|---|
| 40 | `ENV_tree_cypress_33_LOD1` | 7.1 (6.5, 5.9) | **YES** | -2.27 .. +0.10 | -1.81 .. 4.42 | 1 435 | **17.7** |
| 147 | `ENV_tree_redwood_26_LOD1` | 8.2 (6.6, 6.1) | no — one corner clears the near plane (+0.2 m), projects to ndc x -27.8 | – | – | – | – |
| 41 | `ENV_tree_cypress_34_LOD1` | 11.8 (10.4, 10.1) | **YES** | -1.74 .. -0.38 | -0.85 .. 3.03 | 956 | **11.8** |

Both over the 8e believable band (9-13 px), so **option (a) stays rejected**; (b) as worded is empty (all 39 belt trees are 0.2-7.3 m from a walkable surface); **chosen (b) re-cut per set**: a tagged row keeps its mesh only within that set's own `draw_within_m` + the 5 m fade band of a QA station eye.

1. **Walk-up (desktop, 20 m): 4 kept / 35 billboard-only, 4 937 933 -> 3 890 782 placed tris (-1 047 151, -21.2 %), rows 166 -> 131** — removes 1 047 151 of cam03's 1 301 870 excess triangles and ~24 of its 28 draws.
2. **Far (mobile, 50 m): 22 kept / 17 billboard-only, 1 317 097 -> 1 182 338 (-134 759, -10.2 %), rows 166 -> 149** — ~19 draws.
3. **VIEWER HAND-OFF, and it gates the ship: `foliageLazy.js` must re-light a billboard-only impostor.** It builds its impostor complement from the MESH placements, so a row with no mesh placement correctly never gets `iNear = 1` but also never gets `iIrr` — and the belt's median `E_placement / E_bake` is **0.2424**, so those 35 crowns would draw ~4x too bright and QA 23's fix would be undone. The fix is to set `irr` for a row with no mesh placement while leaving `near` at 0; everything it needs is already in the manifest (`trees.far_mesh.lighting.mesh.placements` has a row per far tree with `mesh: true|false`, and `trees.<set>.billboard_only` names the excluded rows). Contract: `web/test/foliage_lazy_test.mjs` section 4c. **Must ship in the same deploy as the rule.**
4. **Pin delta** (`export/p8d_pin.py`): the export set does **not** move (`tree_rule` 186/166/85/20, ENV placed 895 052, arch/orn/ground byte-identical). Six numbers move, all downstream: `trees_far[far|walkup].placements` **149 / 131**, `billboard_only` **17 / 35**, `placed_tris` **1 182 338 / 3 890 782**. 13 pins in all; the subset relation is now checked **by row index**, not by count.
5. **Command list**: verbatim in `export/README.md`, section "The chain the lead runs" (grep `### The chain the lead runs`). Order: the three CPU checks -> `trees_far.py` far then walkup (both, in that order) -> `gltf_pack.sh --trees` / `--trees-lod1` -> `p8d_pin.py` -> `instance_rows.mjs` x2 -> gate4 order + selftest + `verify_glb` -> `manifest_v2/v3/v4` -> `budget_doc` -> `sync_main` + `tiers` x3 -> `verify_glb --gate5` desktop+mobile -> `npm test` + `tiers_test` + `name_sweep` -> `p8d_pin --glbs` (after the pack, never before) -> `sync_main`. The manifest grows **+6 166 B gzipped** (0.8 % of the last deploy's headroom); nothing else grows.

**Done this round** (each its own commit, suite counts in every message): merged main (both 2026-09-20 decisions entries kept); `belt_rule.py --frustum` — the table above became a script, with the invariant *no belt row in a station's frame may be billboard-only in either set*; carry r1 item 4's second half (a `glb.per_class` class that is **not** refreshed now says so, both directions); the pin's subset check by index. Two prose corrections the script forced: px/texel is 17.7 / 11.8 (not 17.9 / 11.9 — rounding), and "d to eye" was the crown-centre distance while the rule measures the trunk base (both now labelled; a centre-point frustum test calls cypress_33 invisible and would have justified option (a)).

**Carries audited as done**: r1 item 2 (`impostors.variant_2k.tier0_stand_in`), r1 item 3 (`PFA_MAIN_ROOT` default, CAM03 read from the manifest, `p8a_shrub_check` exits non-zero), r1 item 4 (`p8c_ktx2_compare.py` + the per_class print), r6 item 7 (a non-`--glbs` run drops the stale `glbs` block and stamps `glbs_note` instead of carrying it).

**Suites, all CPU, all green**: `belt_rule` PASS, `belt_rule --frustum` PASS (2 of 3 in frame), `p9_rule_selftest` **23/23** (16 -> 23; it gained `p8d_pin.mesh_rows` as a third reader), `gate4_order_selftest` **10/10**, `web npm test` **450/450**, `foliage_lazy` 4c PASS. Not runnable here: `verify_glb`, `manifest_v4.main`, `p8d_pin.main`, `tiers` — all need `export/out` artefacts this worktree does not hold; they are in the chain above. **No Blender, no bake, no pack, no deploy, no Chrome** — nothing needed one: every number came from the gate3 manifest and the two `trees_far*.json` reports.
